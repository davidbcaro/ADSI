/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package co.edu.sena.model.dao.jdbc;

import co.edu.sena.model.dao.dao.*;
import co.edu.sena.model.dao.factory.*;
import co.edu.sena.model.dao.dto.*;
import co.edu.sena.model.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class TeacherDaoImpl extends AbstractDAO implements TeacherDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT id_Document, document_Number, type_Linking, name_Specialty FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( id_Document, document_Number, type_Linking, name_Specialty ) VALUES ( ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET id_Document = ?, document_Number = ?, type_Linking = ?, name_Specialty = ? WHERE id_Document = ? AND document_Number = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE id_Document = ? AND document_Number = ?";

	/** 
	 * Index of column id_Document
	 */
	protected static final int COLUMN_ID_DOCUMENT = 1;

	/** 
	 * Index of column document_Number
	 */
	protected static final int COLUMN_DOCUMENT_NUMBER = 2;

	/** 
	 * Index of column type_Linking
	 */
	protected static final int COLUMN_TYPE_LINKING = 3;

	/** 
	 * Index of column name_Specialty
	 */
	protected static final int COLUMN_NAME_SPECIALTY = 4;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 4;

	/** 
	 * Index of primary-key column id_Document
	 */
	protected static final int PK_COLUMN_ID_DOCUMENT = 1;

	/** 
	 * Index of primary-key column document_Number
	 */
	protected static final int PK_COLUMN_DOCUMENT_NUMBER = 2;

	/** 
	 * Inserts a new row in the teacher table.
	 */
	public TeacherPk insert(Teacher dto) throws TeacherDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setString( index++, dto.getIdDocument() );
			stmt.setString( index++, dto.getDocumentNumber() );
			stmt.setString( index++, dto.getTypeLinking() );
			stmt.setString( index++, dto.getNameSpecialty() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TeacherDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the teacher table.
	 */
	public void update(TeacherPk pk, Teacher dto) throws TeacherDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setString( index++, dto.getIdDocument() );
			stmt.setString( index++, dto.getDocumentNumber() );
			stmt.setString( index++, dto.getTypeLinking() );
			stmt.setString( index++, dto.getNameSpecialty() );
			stmt.setString( 5, pk.getIdDocument() );
			stmt.setString( 6, pk.getDocumentNumber() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TeacherDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the teacher table.
	 */
	public void delete(TeacherPk pk) throws TeacherDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setString( 1, pk.getIdDocument() );
			stmt.setString( 2, pk.getDocumentNumber() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TeacherDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the teacher table that matches the specified primary-key value.
	 */
	public Teacher findByPrimaryKey(TeacherPk pk) throws TeacherDaoException
	{
		return findByPrimaryKey( pk.getIdDocument(), pk.getDocumentNumber() );
	}

	/** 
	 * Returns all rows from the teacher table that match the criteria 'id_Document = :idDocument AND document_Number = :documentNumber'.
	 */
	public Teacher findByPrimaryKey(String idDocument, String documentNumber) throws TeacherDaoException
	{
		Teacher ret[] = findByDynamicSelect( SQL_SELECT + " WHERE id_Document = ? AND document_Number = ?", new Object[] { idDocument, documentNumber } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the teacher table that match the criteria ''.
	 */
	public Teacher[] findAll() throws TeacherDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY id_Document, document_Number", null );
	}

	/** 
	 * Returns all rows from the teacher table that match the criteria 'id_Document = :idDocument AND document_Number = :documentNumber'.
	 */
	public Teacher[] findByClient(String idDocument, String documentNumber) throws TeacherDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE id_Document = ? AND document_Number = ?", new Object[] { idDocument, documentNumber } );
	}

	/** 
	 * Returns all rows from the teacher table that match the criteria 'name_Specialty = :nameSpecialty'.
	 */
	public Teacher[] findBySpecialty(String nameSpecialty) throws TeacherDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE name_Specialty = ?", new Object[] { nameSpecialty } );
	}

	/** 
	 * Returns all rows from the teacher table that match the criteria 'type_Linking = :typeLinking'.
	 */
	public Teacher[] findByLinking(String typeLinking) throws TeacherDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE type_Linking = ?", new Object[] { typeLinking } );
	}

	/** 
	 * Returns all rows from the teacher table that match the criteria 'id_Document = :idDocument'.
	 */
	public Teacher[] findWhereIdDocumentEquals(String idDocument) throws TeacherDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE id_Document = ? ORDER BY id_Document", new Object[] { idDocument } );
	}

	/** 
	 * Returns all rows from the teacher table that match the criteria 'document_Number = :documentNumber'.
	 */
	public Teacher[] findWhereDocumentNumberEquals(String documentNumber) throws TeacherDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE document_Number = ? ORDER BY document_Number", new Object[] { documentNumber } );
	}

	/** 
	 * Returns all rows from the teacher table that match the criteria 'type_Linking = :typeLinking'.
	 */
	public Teacher[] findWhereTypeLinkingEquals(String typeLinking) throws TeacherDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE type_Linking = ? ORDER BY type_Linking", new Object[] { typeLinking } );
	}

	/** 
	 * Returns all rows from the teacher table that match the criteria 'name_Specialty = :nameSpecialty'.
	 */
	public Teacher[] findWhereNameSpecialtyEquals(String nameSpecialty) throws TeacherDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE name_Specialty = ? ORDER BY name_Specialty", new Object[] { nameSpecialty } );
	}

	/**
	 * Method 'TeacherDaoImpl'
	 * 
	 */
	public TeacherDaoImpl()
	{
	}

	/**
	 * Method 'TeacherDaoImpl'
	 * 
	 * @param userConn
	 */
	public TeacherDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "wariosoft.teacher";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected Teacher fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			Teacher dto = new Teacher();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected Teacher[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			Teacher dto = new Teacher();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		Teacher ret[] = new Teacher[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(Teacher dto, ResultSet rs) throws SQLException
	{
		dto.setIdDocument( rs.getString( COLUMN_ID_DOCUMENT ) );
		dto.setDocumentNumber( rs.getString( COLUMN_DOCUMENT_NUMBER ) );
		dto.setTypeLinking( rs.getString( COLUMN_TYPE_LINKING ) );
		dto.setNameSpecialty( rs.getString( COLUMN_NAME_SPECIALTY ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(Teacher dto)
	{
	}

	/** 
	 * Returns all rows from the teacher table that match the specified arbitrary SQL statement
	 */
	public Teacher[] findByDynamicSelect(String sql, Object[] sqlParams) throws TeacherDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TeacherDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the teacher table that match the specified arbitrary SQL statement
	 */
	public Teacher[] findByDynamicWhere(String sql, Object[] sqlParams) throws TeacherDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TeacherDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
