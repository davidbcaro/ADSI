/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package co.edu.sena.model.dao.jdbc;

import co.edu.sena.model.dao.dao.*;
import co.edu.sena.model.dao.factory.*;
import co.edu.sena.model.dao.dto.*;
import co.edu.sena.model.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class VersionDaoImpl extends AbstractDAO implements VersionDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT id_Version, active, id_Current_Quarter FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( id_Version, active, id_Current_Quarter ) VALUES ( ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET id_Version = ?, active = ?, id_Current_Quarter = ? WHERE id_Version = ? AND id_Current_Quarter = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE id_Version = ? AND id_Current_Quarter = ?";

	/** 
	 * Index of column id_Version
	 */
	protected static final int COLUMN_ID_VERSION = 1;

	/** 
	 * Index of column active
	 */
	protected static final int COLUMN_ACTIVE = 2;

	/** 
	 * Index of column id_Current_Quarter
	 */
	protected static final int COLUMN_ID_CURRENT_QUARTER = 3;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 3;

	/** 
	 * Index of primary-key column id_Version
	 */
	protected static final int PK_COLUMN_ID_VERSION = 1;

	/** 
	 * Index of primary-key column id_Current_Quarter
	 */
	protected static final int PK_COLUMN_ID_CURRENT_QUARTER = 2;

	/** 
	 * Inserts a new row in the version table.
	 */
	public VersionPk insert(Version dto) throws VersionDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setInt( index++, dto.getIdVersion() );
			if (dto.isActiveNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setShort( index++, dto.getActive() );
			}
		
			stmt.setString( index++, dto.getIdCurrentQuarter() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new VersionDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the version table.
	 */
	public void update(VersionPk pk, Version dto) throws VersionDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setInt( index++, dto.getIdVersion() );
			if (dto.isActiveNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setShort( index++, dto.getActive() );
			}
		
			stmt.setString( index++, dto.getIdCurrentQuarter() );
			stmt.setInt( 4, pk.getIdVersion() );
			stmt.setString( 5, pk.getIdCurrentQuarter() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new VersionDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the version table.
	 */
	public void delete(VersionPk pk) throws VersionDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getIdVersion() );
			stmt.setString( 2, pk.getIdCurrentQuarter() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new VersionDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the version table that matches the specified primary-key value.
	 */
	public Version findByPrimaryKey(VersionPk pk) throws VersionDaoException
	{
		return findByPrimaryKey( pk.getIdVersion(), pk.getIdCurrentQuarter() );
	}

	/** 
	 * Returns all rows from the version table that match the criteria 'id_Version = :idVersion AND id_Current_Quarter = :idCurrentQuarter'.
	 */
	public Version findByPrimaryKey(int idVersion, String idCurrentQuarter) throws VersionDaoException
	{
		Version ret[] = findByDynamicSelect( SQL_SELECT + " WHERE id_Version = ? AND id_Current_Quarter = ?", new Object[] {  new Integer(idVersion), idCurrentQuarter } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the version table that match the criteria ''.
	 */
	public Version[] findAll() throws VersionDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY id_Version, id_Current_Quarter", null );
	}

	/** 
	 * Returns all rows from the version table that match the criteria 'id_Current_Quarter = :idCurrentQuarter'.
	 */
	public Version[] findByCurrentQuarter(String idCurrentQuarter) throws VersionDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE id_Current_Quarter = ?", new Object[] { idCurrentQuarter } );
	}

	/** 
	 * Returns all rows from the version table that match the criteria 'id_Version = :idVersion'.
	 */
	public Version[] findWhereIdVersionEquals(int idVersion) throws VersionDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE id_Version = ? ORDER BY id_Version", new Object[] {  new Integer(idVersion) } );
	}

	/** 
	 * Returns all rows from the version table that match the criteria 'active = :active'.
	 */
	public Version[] findWhereActiveEquals(short active) throws VersionDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE active = ? ORDER BY active", new Object[] {  new Short(active) } );
	}

	/** 
	 * Returns all rows from the version table that match the criteria 'id_Current_Quarter = :idCurrentQuarter'.
	 */
	public Version[] findWhereIdCurrentQuarterEquals(String idCurrentQuarter) throws VersionDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE id_Current_Quarter = ? ORDER BY id_Current_Quarter", new Object[] { idCurrentQuarter } );
	}

	/**
	 * Method 'VersionDaoImpl'
	 * 
	 */
	public VersionDaoImpl()
	{
	}

	/**
	 * Method 'VersionDaoImpl'
	 * 
	 * @param userConn
	 */
	public VersionDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "wariosoft.version";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected Version fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			Version dto = new Version();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected Version[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			Version dto = new Version();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		Version ret[] = new Version[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(Version dto, ResultSet rs) throws SQLException
	{
		dto.setIdVersion( rs.getInt( COLUMN_ID_VERSION ) );
		dto.setActive( rs.getShort( COLUMN_ACTIVE ) );
		if (rs.wasNull()) {
			dto.setActiveNull( true );
		}
		
		dto.setIdCurrentQuarter( rs.getString( COLUMN_ID_CURRENT_QUARTER ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(Version dto)
	{
	}

	/** 
	 * Returns all rows from the version table that match the specified arbitrary SQL statement
	 */
	public Version[] findByDynamicSelect(String sql, Object[] sqlParams) throws VersionDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new VersionDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the version table that match the specified arbitrary SQL statement
	 */
	public Version[] findByDynamicWhere(String sql, Object[] sqlParams) throws VersionDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new VersionDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
