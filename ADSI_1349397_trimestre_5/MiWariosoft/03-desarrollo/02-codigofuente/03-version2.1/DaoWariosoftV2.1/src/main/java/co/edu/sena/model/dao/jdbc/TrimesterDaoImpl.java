/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package co.edu.sena.model.dao.jdbc;

import co.edu.sena.model.dao.dao.*;
import co.edu.sena.model.dao.factory.*;
import co.edu.sena.model.dao.dto.*;
import co.edu.sena.model.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class TrimesterDaoImpl extends AbstractDAO implements TrimesterDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT name_Trimester, training_Level, initials_Working_Day FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( name_Trimester, training_Level, initials_Working_Day ) VALUES ( ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET name_Trimester = ?, training_Level = ?, initials_Working_Day = ? WHERE name_Trimester = ? AND initials_Working_Day = ? AND training_Level = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE name_Trimester = ? AND initials_Working_Day = ? AND training_Level = ?";

	/** 
	 * Index of column name_Trimester
	 */
	protected static final int COLUMN_NAME_TRIMESTER = 1;

	/** 
	 * Index of column training_Level
	 */
	protected static final int COLUMN_TRAINING_LEVEL = 2;

	/** 
	 * Index of column initials_Working_Day
	 */
	protected static final int COLUMN_INITIALS_WORKING_DAY = 3;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 3;

	/** 
	 * Index of primary-key column name_Trimester
	 */
	protected static final int PK_COLUMN_NAME_TRIMESTER = 1;

	/** 
	 * Index of primary-key column initials_Working_Day
	 */
	protected static final int PK_COLUMN_INITIALS_WORKING_DAY = 2;

	/** 
	 * Index of primary-key column training_Level
	 */
	protected static final int PK_COLUMN_TRAINING_LEVEL = 3;

	/** 
	 * Inserts a new row in the trimester table.
	 */
	public TrimesterPk insert(Trimester dto) throws TrimesterDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setString( index++, dto.getNameTrimester() );
			stmt.setString( index++, dto.getTrainingLevel() );
			stmt.setString( index++, dto.getInitialsWorkingDay() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TrimesterDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the trimester table.
	 */
	public void update(TrimesterPk pk, Trimester dto) throws TrimesterDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setString( index++, dto.getNameTrimester() );
			stmt.setString( index++, dto.getTrainingLevel() );
			stmt.setString( index++, dto.getInitialsWorkingDay() );
			stmt.setString( 4, pk.getNameTrimester() );
			stmt.setString( 5, pk.getInitialsWorkingDay() );
			stmt.setString( 6, pk.getTrainingLevel() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TrimesterDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the trimester table.
	 */
	public void delete(TrimesterPk pk) throws TrimesterDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setString( 1, pk.getNameTrimester() );
			stmt.setString( 2, pk.getInitialsWorkingDay() );
			stmt.setString( 3, pk.getTrainingLevel() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TrimesterDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the trimester table that matches the specified primary-key value.
	 */
	public Trimester findByPrimaryKey(TrimesterPk pk) throws TrimesterDaoException
	{
		return findByPrimaryKey( pk.getNameTrimester(), pk.getInitialsWorkingDay(), pk.getTrainingLevel() );
	}

	/** 
	 * Returns all rows from the trimester table that match the criteria 'name_Trimester = :nameTrimester AND initials_Working_Day = :initialsWorkingDay AND training_Level = :trainingLevel'.
	 */
	public Trimester findByPrimaryKey(String nameTrimester, String initialsWorkingDay, String trainingLevel) throws TrimesterDaoException
	{
		Trimester ret[] = findByDynamicSelect( SQL_SELECT + " WHERE name_Trimester = ? AND initials_Working_Day = ? AND training_Level = ?", new Object[] { nameTrimester, initialsWorkingDay, trainingLevel } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the trimester table that match the criteria ''.
	 */
	public Trimester[] findAll() throws TrimesterDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY name_Trimester, initials_Working_Day, training_Level", null );
	}

	/** 
	 * Returns all rows from the trimester table that match the criteria 'training_Level = :trainingLevel'.
	 */
	public Trimester[] findByTrainingLevel(String trainingLevel) throws TrimesterDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE training_Level = ?", new Object[] { trainingLevel } );
	}

	/** 
	 * Returns all rows from the trimester table that match the criteria 'initials_Working_Day = :initialsWorkingDay'.
	 */
	public Trimester[] findByWorkingDay(String initialsWorkingDay) throws TrimesterDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE initials_Working_Day = ?", new Object[] { initialsWorkingDay } );
	}

	/** 
	 * Returns all rows from the trimester table that match the criteria 'name_Trimester = :nameTrimester'.
	 */
	public Trimester[] findWhereNameTrimesterEquals(String nameTrimester) throws TrimesterDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE name_Trimester = ? ORDER BY name_Trimester", new Object[] { nameTrimester } );
	}

	/** 
	 * Returns all rows from the trimester table that match the criteria 'training_Level = :trainingLevel'.
	 */
	public Trimester[] findWhereTrainingLevelEquals(String trainingLevel) throws TrimesterDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE training_Level = ? ORDER BY training_Level", new Object[] { trainingLevel } );
	}

	/** 
	 * Returns all rows from the trimester table that match the criteria 'initials_Working_Day = :initialsWorkingDay'.
	 */
	public Trimester[] findWhereInitialsWorkingDayEquals(String initialsWorkingDay) throws TrimesterDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE initials_Working_Day = ? ORDER BY initials_Working_Day", new Object[] { initialsWorkingDay } );
	}

	/**
	 * Method 'TrimesterDaoImpl'
	 * 
	 */
	public TrimesterDaoImpl()
	{
	}

	/**
	 * Method 'TrimesterDaoImpl'
	 * 
	 * @param userConn
	 */
	public TrimesterDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "wariosoft.trimester";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected Trimester fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			Trimester dto = new Trimester();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected Trimester[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			Trimester dto = new Trimester();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		Trimester ret[] = new Trimester[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(Trimester dto, ResultSet rs) throws SQLException
	{
		dto.setNameTrimester( rs.getString( COLUMN_NAME_TRIMESTER ) );
		dto.setTrainingLevel( rs.getString( COLUMN_TRAINING_LEVEL ) );
		dto.setInitialsWorkingDay( rs.getString( COLUMN_INITIALS_WORKING_DAY ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(Trimester dto)
	{
	}

	/** 
	 * Returns all rows from the trimester table that match the specified arbitrary SQL statement
	 */
	public Trimester[] findByDynamicSelect(String sql, Object[] sqlParams) throws TrimesterDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TrimesterDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the trimester table that match the specified arbitrary SQL statement
	 */
	public Trimester[] findByDynamicWhere(String sql, Object[] sqlParams) throws TrimesterDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TrimesterDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
