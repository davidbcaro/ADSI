/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package co.edu.sena.model.dao.jdbc;

import co.edu.sena.model.dao.dao.*;
import co.edu.sena.model.dao.factory.*;
import java.util.Date;
import co.edu.sena.model.dao.dto.*;
import co.edu.sena.model.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class NumberGroupDaoImpl extends AbstractDAO implements NumberGroupDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT number_Group, start_Date, end_Date, route, name_State, program_Code, version FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( number_Group, start_Date, end_Date, route, name_State, program_Code, version ) VALUES ( ?, ?, ?, ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET number_Group = ?, start_Date = ?, end_Date = ?, route = ?, name_State = ?, program_Code = ?, version = ? WHERE number_Group = ? AND version = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE number_Group = ? AND version = ?";

	/** 
	 * Index of column number_Group
	 */
	protected static final int COLUMN_NUMBER_GROUP = 1;

	/** 
	 * Index of column start_Date
	 */
	protected static final int COLUMN_START_DATE = 2;

	/** 
	 * Index of column end_Date
	 */
	protected static final int COLUMN_END_DATE = 3;

	/** 
	 * Index of column route
	 */
	protected static final int COLUMN_ROUTE = 4;

	/** 
	 * Index of column name_State
	 */
	protected static final int COLUMN_NAME_STATE = 5;

	/** 
	 * Index of column program_Code
	 */
	protected static final int COLUMN_PROGRAM_CODE = 6;

	/** 
	 * Index of column version
	 */
	protected static final int COLUMN_VERSION = 7;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 7;

	/** 
	 * Index of primary-key column number_Group
	 */
	protected static final int PK_COLUMN_NUMBER_GROUP = 1;

	/** 
	 * Index of primary-key column version
	 */
	protected static final int PK_COLUMN_VERSION = 2;

	/** 
	 * Inserts a new row in the number_group table.
	 */
	public NumberGroupPk insert(NumberGroup dto) throws NumberGroupDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setString( index++, dto.getNumberGroup() );
			stmt.setDate(index++, dto.getStartDate()==null ? null : new java.sql.Date( dto.getStartDate().getTime() ) );
			stmt.setDate(index++, dto.getEndDate()==null ? null : new java.sql.Date( dto.getEndDate().getTime() ) );
			stmt.setString( index++, dto.getRoute() );
			stmt.setString( index++, dto.getNameState() );
			stmt.setString( index++, dto.getProgramCode() );
			stmt.setString( index++, dto.getVersion() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new NumberGroupDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the number_group table.
	 */
	public void update(NumberGroupPk pk, NumberGroup dto) throws NumberGroupDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setString( index++, dto.getNumberGroup() );
			stmt.setDate(index++, dto.getStartDate()==null ? null : new java.sql.Date( dto.getStartDate().getTime() ) );
			stmt.setDate(index++, dto.getEndDate()==null ? null : new java.sql.Date( dto.getEndDate().getTime() ) );
			stmt.setString( index++, dto.getRoute() );
			stmt.setString( index++, dto.getNameState() );
			stmt.setString( index++, dto.getProgramCode() );
			stmt.setString( index++, dto.getVersion() );
			stmt.setString( 8, pk.getNumberGroup() );
			stmt.setString( 9, pk.getVersion() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new NumberGroupDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the number_group table.
	 */
	public void delete(NumberGroupPk pk) throws NumberGroupDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setString( 1, pk.getNumberGroup() );
			stmt.setString( 2, pk.getVersion() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new NumberGroupDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the number_group table that matches the specified primary-key value.
	 */
	public NumberGroup findByPrimaryKey(NumberGroupPk pk) throws NumberGroupDaoException
	{
		return findByPrimaryKey( pk.getNumberGroup(), pk.getVersion() );
	}

	/** 
	 * Returns all rows from the number_group table that match the criteria 'number_Group = :numberGroup AND version = :version'.
	 */
	public NumberGroup findByPrimaryKey(String numberGroup, String version) throws NumberGroupDaoException
	{
		NumberGroup ret[] = findByDynamicSelect( SQL_SELECT + " WHERE number_Group = ? AND version = ?", new Object[] { numberGroup, version } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the number_group table that match the criteria ''.
	 */
	public NumberGroup[] findAll() throws NumberGroupDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY number_Group, version", null );
	}

	/** 
	 * Returns all rows from the number_group table that match the criteria 'program_Code = :programCode AND version = :version'.
	 */
	public NumberGroup[] findByProgram(String programCode, String version) throws NumberGroupDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE program_Code = ? AND version = ?", new Object[] { programCode, version } );
	}

	/** 
	 * Returns all rows from the number_group table that match the criteria 'name_State = :nameState'.
	 */
	public NumberGroup[] findByStateNumberGroup(String nameState) throws NumberGroupDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE name_State = ?", new Object[] { nameState } );
	}

	/** 
	 * Returns all rows from the number_group table that match the criteria 'number_Group = :numberGroup'.
	 */
	public NumberGroup[] findWhereNumberGroupEquals(String numberGroup) throws NumberGroupDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE number_Group = ? ORDER BY number_Group", new Object[] { numberGroup } );
	}

	/** 
	 * Returns all rows from the number_group table that match the criteria 'start_Date = :startDate'.
	 */
	public NumberGroup[] findWhereStartDateEquals(Date startDate) throws NumberGroupDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE start_Date = ? ORDER BY start_Date", new Object[] { startDate==null ? null : new java.sql.Date( startDate.getTime() ) } );
	}

	/** 
	 * Returns all rows from the number_group table that match the criteria 'end_Date = :endDate'.
	 */
	public NumberGroup[] findWhereEndDateEquals(Date endDate) throws NumberGroupDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE end_Date = ? ORDER BY end_Date", new Object[] { endDate==null ? null : new java.sql.Date( endDate.getTime() ) } );
	}

	/** 
	 * Returns all rows from the number_group table that match the criteria 'route = :route'.
	 */
	public NumberGroup[] findWhereRouteEquals(String route) throws NumberGroupDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE route = ? ORDER BY route", new Object[] { route } );
	}

	/** 
	 * Returns all rows from the number_group table that match the criteria 'name_State = :nameState'.
	 */
	public NumberGroup[] findWhereNameStateEquals(String nameState) throws NumberGroupDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE name_State = ? ORDER BY name_State", new Object[] { nameState } );
	}

	/** 
	 * Returns all rows from the number_group table that match the criteria 'program_Code = :programCode'.
	 */
	public NumberGroup[] findWhereProgramCodeEquals(String programCode) throws NumberGroupDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE program_Code = ? ORDER BY program_Code", new Object[] { programCode } );
	}

	/** 
	 * Returns all rows from the number_group table that match the criteria 'version = :version'.
	 */
	public NumberGroup[] findWhereVersionEquals(String version) throws NumberGroupDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE version = ? ORDER BY version", new Object[] { version } );
	}

	/**
	 * Method 'NumberGroupDaoImpl'
	 * 
	 */
	public NumberGroupDaoImpl()
	{
	}

	/**
	 * Method 'NumberGroupDaoImpl'
	 * 
	 * @param userConn
	 */
	public NumberGroupDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "wariosoft.number_group";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected NumberGroup fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			NumberGroup dto = new NumberGroup();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected NumberGroup[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			NumberGroup dto = new NumberGroup();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		NumberGroup ret[] = new NumberGroup[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(NumberGroup dto, ResultSet rs) throws SQLException
	{
		dto.setNumberGroup( rs.getString( COLUMN_NUMBER_GROUP ) );
		dto.setStartDate( rs.getDate(COLUMN_START_DATE ) );
		dto.setEndDate( rs.getDate(COLUMN_END_DATE ) );
		dto.setRoute( rs.getString( COLUMN_ROUTE ) );
		dto.setNameState( rs.getString( COLUMN_NAME_STATE ) );
		dto.setProgramCode( rs.getString( COLUMN_PROGRAM_CODE ) );
		dto.setVersion( rs.getString( COLUMN_VERSION ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(NumberGroup dto)
	{
	}

	/** 
	 * Returns all rows from the number_group table that match the specified arbitrary SQL statement
	 */
	public NumberGroup[] findByDynamicSelect(String sql, Object[] sqlParams) throws NumberGroupDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new NumberGroupDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the number_group table that match the specified arbitrary SQL statement
	 */
	public NumberGroup[] findByDynamicWhere(String sql, Object[] sqlParams) throws NumberGroupDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new NumberGroupDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
