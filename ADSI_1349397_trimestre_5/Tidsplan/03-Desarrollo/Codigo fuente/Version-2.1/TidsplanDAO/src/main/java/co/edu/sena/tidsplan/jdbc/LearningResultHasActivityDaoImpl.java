/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package co.edu.sena.tidsplan.jdbc;

import co.edu.sena.tidsplan.dao.*;
import co.edu.sena.tidsplan.factory.*;
import co.edu.sena.tidsplan.dto.*;
import co.edu.sena.tidsplan.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class LearningResultHasActivityDaoImpl extends AbstractDAO implements LearningResultHasActivityDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT lr_code, program_code, competition_code, id_activity, name_phase, project_code, program_version FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( lr_code, program_code, competition_code, id_activity, name_phase, project_code, program_version ) VALUES ( ?, ?, ?, ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET lr_code = ?, program_code = ?, competition_code = ?, id_activity = ?, name_phase = ?, project_code = ?, program_version = ? WHERE lr_code = ? AND program_code = ? AND competition_code = ? AND id_activity = ? AND name_phase = ? AND project_code = ? AND program_version = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE lr_code = ? AND program_code = ? AND competition_code = ? AND id_activity = ? AND name_phase = ? AND project_code = ? AND program_version = ?";

	/** 
	 * Index of column lr_code
	 */
	protected static final int COLUMN_LR_CODE = 1;

	/** 
	 * Index of column program_code
	 */
	protected static final int COLUMN_PROGRAM_CODE = 2;

	/** 
	 * Index of column competition_code
	 */
	protected static final int COLUMN_COMPETITION_CODE = 3;

	/** 
	 * Index of column id_activity
	 */
	protected static final int COLUMN_ID_ACTIVITY = 4;

	/** 
	 * Index of column name_phase
	 */
	protected static final int COLUMN_NAME_PHASE = 5;

	/** 
	 * Index of column project_code
	 */
	protected static final int COLUMN_PROJECT_CODE = 6;

	/** 
	 * Index of column program_version
	 */
	protected static final int COLUMN_PROGRAM_VERSION = 7;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 7;

	/** 
	 * Index of primary-key column lr_code
	 */
	protected static final int PK_COLUMN_LR_CODE = 1;

	/** 
	 * Index of primary-key column program_code
	 */
	protected static final int PK_COLUMN_PROGRAM_CODE = 2;

	/** 
	 * Index of primary-key column competition_code
	 */
	protected static final int PK_COLUMN_COMPETITION_CODE = 3;

	/** 
	 * Index of primary-key column id_activity
	 */
	protected static final int PK_COLUMN_ID_ACTIVITY = 4;

	/** 
	 * Index of primary-key column name_phase
	 */
	protected static final int PK_COLUMN_NAME_PHASE = 5;

	/** 
	 * Index of primary-key column project_code
	 */
	protected static final int PK_COLUMN_PROJECT_CODE = 6;

	/** 
	 * Index of primary-key column program_version
	 */
	protected static final int PK_COLUMN_PROGRAM_VERSION = 7;

	/** 
	 * Inserts a new row in the learning_result_has_activity table.
	 */
	public LearningResultHasActivityPk insert(LearningResultHasActivity dto) throws LearningResultHasActivityDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setString( index++, dto.getLrCode() );
			stmt.setString( index++, dto.getProgramCode() );
			stmt.setString( index++, dto.getCompetitionCode() );
			stmt.setString( index++, dto.getIdActivity() );
			stmt.setString( index++, dto.getNamePhase() );
			stmt.setString( index++, dto.getProjectCode() );
			stmt.setString( index++, dto.getProgramVersion() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new LearningResultHasActivityDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the learning_result_has_activity table.
	 */
	public void update(LearningResultHasActivityPk pk, LearningResultHasActivity dto) throws LearningResultHasActivityDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setString( index++, dto.getLrCode() );
			stmt.setString( index++, dto.getProgramCode() );
			stmt.setString( index++, dto.getCompetitionCode() );
			stmt.setString( index++, dto.getIdActivity() );
			stmt.setString( index++, dto.getNamePhase() );
			stmt.setString( index++, dto.getProjectCode() );
			stmt.setString( index++, dto.getProgramVersion() );
			stmt.setString( 8, pk.getLrCode() );
			stmt.setString( 9, pk.getProgramCode() );
			stmt.setString( 10, pk.getCompetitionCode() );
			stmt.setString( 11, pk.getIdActivity() );
			stmt.setString( 12, pk.getNamePhase() );
			stmt.setString( 13, pk.getProjectCode() );
			stmt.setString( 14, pk.getProgramVersion() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new LearningResultHasActivityDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the learning_result_has_activity table.
	 */
	public void delete(LearningResultHasActivityPk pk) throws LearningResultHasActivityDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setString( 1, pk.getLrCode() );
			stmt.setString( 2, pk.getProgramCode() );
			stmt.setString( 3, pk.getCompetitionCode() );
			stmt.setString( 4, pk.getIdActivity() );
			stmt.setString( 5, pk.getNamePhase() );
			stmt.setString( 6, pk.getProjectCode() );
			stmt.setString( 7, pk.getProgramVersion() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new LearningResultHasActivityDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the learning_result_has_activity table that matches the specified primary-key value.
	 */
	public LearningResultHasActivity findByPrimaryKey(LearningResultHasActivityPk pk) throws LearningResultHasActivityDaoException
	{
		return findByPrimaryKey( pk.getLrCode(), pk.getProgramCode(), pk.getCompetitionCode(), pk.getIdActivity(), pk.getNamePhase(), pk.getProjectCode(), pk.getProgramVersion() );
	}

	/** 
	 * Returns all rows from the learning_result_has_activity table that match the criteria 'lr_code = :lrCode AND program_code = :programCode AND competition_code = :competitionCode AND id_activity = :idActivity AND name_phase = :namePhase AND project_code = :projectCode AND program_version = :programVersion'.
	 */
	public LearningResultHasActivity findByPrimaryKey(String lrCode, String programCode, String competitionCode, String idActivity, String namePhase, String projectCode, String programVersion) throws LearningResultHasActivityDaoException
	{
		LearningResultHasActivity ret[] = findByDynamicSelect( SQL_SELECT + " WHERE lr_code = ? AND program_code = ? AND competition_code = ? AND id_activity = ? AND name_phase = ? AND project_code = ? AND program_version = ?", new Object[] { lrCode, programCode, competitionCode, idActivity, namePhase, projectCode, programVersion } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the learning_result_has_activity table that match the criteria ''.
	 */
	public LearningResultHasActivity[] findAll() throws LearningResultHasActivityDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY lr_code, program_code, competition_code, id_activity, name_phase, project_code, program_version", null );
	}

	/** 
	 * Returns all rows from the learning_result_has_activity table that match the criteria 'id_activity = :idActivity AND name_phase = :namePhase AND project_code = :projectCode'.
	 */
	public LearningResultHasActivity[] findByActivity(String idActivity, String namePhase, String projectCode) throws LearningResultHasActivityDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE id_activity = ? AND name_phase = ? AND project_code = ?", new Object[] { idActivity, namePhase, projectCode } );
	}

	/** 
	 * Returns all rows from the learning_result_has_activity table that match the criteria 'lr_code = :lrCode AND program_code = :programCode AND competition_code = :competitionCode AND program_version = :programVersion'.
	 */
	public LearningResultHasActivity[] findByLearningResult(String lrCode, String programCode, String competitionCode, String programVersion) throws LearningResultHasActivityDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE lr_code = ? AND program_code = ? AND competition_code = ? AND program_version = ?", new Object[] { lrCode, programCode, competitionCode, programVersion } );
	}

	/** 
	 * Returns all rows from the learning_result_has_activity table that match the criteria 'lr_code = :lrCode'.
	 */
	public LearningResultHasActivity[] findWhereLrCodeEquals(String lrCode) throws LearningResultHasActivityDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE lr_code = ? ORDER BY lr_code", new Object[] { lrCode } );
	}

	/** 
	 * Returns all rows from the learning_result_has_activity table that match the criteria 'program_code = :programCode'.
	 */
	public LearningResultHasActivity[] findWhereProgramCodeEquals(String programCode) throws LearningResultHasActivityDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE program_code = ? ORDER BY program_code", new Object[] { programCode } );
	}

	/** 
	 * Returns all rows from the learning_result_has_activity table that match the criteria 'competition_code = :competitionCode'.
	 */
	public LearningResultHasActivity[] findWhereCompetitionCodeEquals(String competitionCode) throws LearningResultHasActivityDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE competition_code = ? ORDER BY competition_code", new Object[] { competitionCode } );
	}

	/** 
	 * Returns all rows from the learning_result_has_activity table that match the criteria 'id_activity = :idActivity'.
	 */
	public LearningResultHasActivity[] findWhereIdActivityEquals(String idActivity) throws LearningResultHasActivityDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE id_activity = ? ORDER BY id_activity", new Object[] { idActivity } );
	}

	/** 
	 * Returns all rows from the learning_result_has_activity table that match the criteria 'name_phase = :namePhase'.
	 */
	public LearningResultHasActivity[] findWhereNamePhaseEquals(String namePhase) throws LearningResultHasActivityDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE name_phase = ? ORDER BY name_phase", new Object[] { namePhase } );
	}

	/** 
	 * Returns all rows from the learning_result_has_activity table that match the criteria 'project_code = :projectCode'.
	 */
	public LearningResultHasActivity[] findWhereProjectCodeEquals(String projectCode) throws LearningResultHasActivityDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE project_code = ? ORDER BY project_code", new Object[] { projectCode } );
	}

	/** 
	 * Returns all rows from the learning_result_has_activity table that match the criteria 'program_version = :programVersion'.
	 */
	public LearningResultHasActivity[] findWhereProgramVersionEquals(String programVersion) throws LearningResultHasActivityDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE program_version = ? ORDER BY program_version", new Object[] { programVersion } );
	}

	/**
	 * Method 'LearningResultHasActivityDaoImpl'
	 * 
	 */
	public LearningResultHasActivityDaoImpl()
	{
	}

	/**
	 * Method 'LearningResultHasActivityDaoImpl'
	 * 
	 * @param userConn
	 */
	public LearningResultHasActivityDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "tidsplan.learning_result_has_activity";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected LearningResultHasActivity fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			LearningResultHasActivity dto = new LearningResultHasActivity();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected LearningResultHasActivity[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			LearningResultHasActivity dto = new LearningResultHasActivity();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		LearningResultHasActivity ret[] = new LearningResultHasActivity[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(LearningResultHasActivity dto, ResultSet rs) throws SQLException
	{
		dto.setLrCode( rs.getString( COLUMN_LR_CODE ) );
		dto.setProgramCode( rs.getString( COLUMN_PROGRAM_CODE ) );
		dto.setCompetitionCode( rs.getString( COLUMN_COMPETITION_CODE ) );
		dto.setIdActivity( rs.getString( COLUMN_ID_ACTIVITY ) );
		dto.setNamePhase( rs.getString( COLUMN_NAME_PHASE ) );
		dto.setProjectCode( rs.getString( COLUMN_PROJECT_CODE ) );
		dto.setProgramVersion( rs.getString( COLUMN_PROGRAM_VERSION ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(LearningResultHasActivity dto)
	{
	}

	/** 
	 * Returns all rows from the learning_result_has_activity table that match the specified arbitrary SQL statement
	 */
	public LearningResultHasActivity[] findByDynamicSelect(String sql, Object[] sqlParams) throws LearningResultHasActivityDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new LearningResultHasActivityDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the learning_result_has_activity table that match the specified arbitrary SQL statement
	 */
	public LearningResultHasActivity[] findByDynamicWhere(String sql, Object[] sqlParams) throws LearningResultHasActivityDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new LearningResultHasActivityDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
