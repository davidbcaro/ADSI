/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package co.edu.sena.tidsplan.jdbc;

import co.edu.sena.tidsplan.dao.*;
import co.edu.sena.tidsplan.factory.*;
import java.util.Date;
import co.edu.sena.tidsplan.dto.*;
import co.edu.sena.tidsplan.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class TrimesterCurrentDaoImpl extends AbstractDAO implements TrimesterCurrentDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT id_trimester, start_date, end_date FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( id_trimester, start_date, end_date ) VALUES ( ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET id_trimester = ?, start_date = ?, end_date = ? WHERE id_trimester = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE id_trimester = ?";

	/** 
	 * Index of column id_trimester
	 */
	protected static final int COLUMN_ID_TRIMESTER = 1;

	/** 
	 * Index of column start_date
	 */
	protected static final int COLUMN_START_DATE = 2;

	/** 
	 * Index of column end_date
	 */
	protected static final int COLUMN_END_DATE = 3;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 3;

	/** 
	 * Index of primary-key column id_trimester
	 */
	protected static final int PK_COLUMN_ID_TRIMESTER = 1;

	/** 
	 * Inserts a new row in the trimester_current table.
	 */
	public TrimesterCurrentPk insert(TrimesterCurrent dto) throws TrimesterCurrentDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setString( index++, dto.getIdTrimester() );
			stmt.setDate(index++, dto.getStartDate()==null ? null : new java.sql.Date( dto.getStartDate().getTime() ) );
			stmt.setDate(index++, dto.getEndDate()==null ? null : new java.sql.Date( dto.getEndDate().getTime() ) );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TrimesterCurrentDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the trimester_current table.
	 */
	public void update(TrimesterCurrentPk pk, TrimesterCurrent dto) throws TrimesterCurrentDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setString( index++, dto.getIdTrimester() );
			stmt.setDate(index++, dto.getStartDate()==null ? null : new java.sql.Date( dto.getStartDate().getTime() ) );
			stmt.setDate(index++, dto.getEndDate()==null ? null : new java.sql.Date( dto.getEndDate().getTime() ) );
			stmt.setString( 4, pk.getIdTrimester() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TrimesterCurrentDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the trimester_current table.
	 */
	public void delete(TrimesterCurrentPk pk) throws TrimesterCurrentDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setString( 1, pk.getIdTrimester() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TrimesterCurrentDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the trimester_current table that matches the specified primary-key value.
	 */
	public TrimesterCurrent findByPrimaryKey(TrimesterCurrentPk pk) throws TrimesterCurrentDaoException
	{
		return findByPrimaryKey( pk.getIdTrimester() );
	}

	/** 
	 * Returns all rows from the trimester_current table that match the criteria 'id_trimester = :idTrimester'.
	 */
	public TrimesterCurrent findByPrimaryKey(String idTrimester) throws TrimesterCurrentDaoException
	{
		TrimesterCurrent ret[] = findByDynamicSelect( SQL_SELECT + " WHERE id_trimester = ?", new Object[] { idTrimester } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the trimester_current table that match the criteria ''.
	 */
	public TrimesterCurrent[] findAll() throws TrimesterCurrentDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY id_trimester", null );
	}

	/** 
	 * Returns all rows from the trimester_current table that match the criteria 'id_trimester = :idTrimester'.
	 */
	public TrimesterCurrent[] findWhereIdTrimesterEquals(String idTrimester) throws TrimesterCurrentDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE id_trimester = ? ORDER BY id_trimester", new Object[] { idTrimester } );
	}

	/** 
	 * Returns all rows from the trimester_current table that match the criteria 'start_date = :startDate'.
	 */
	public TrimesterCurrent[] findWhereStartDateEquals(Date startDate) throws TrimesterCurrentDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE start_date = ? ORDER BY start_date", new Object[] { startDate==null ? null : new java.sql.Date( startDate.getTime() ) } );
	}

	/** 
	 * Returns all rows from the trimester_current table that match the criteria 'end_date = :endDate'.
	 */
	public TrimesterCurrent[] findWhereEndDateEquals(Date endDate) throws TrimesterCurrentDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE end_date = ? ORDER BY end_date", new Object[] { endDate==null ? null : new java.sql.Date( endDate.getTime() ) } );
	}

	/**
	 * Method 'TrimesterCurrentDaoImpl'
	 * 
	 */
	public TrimesterCurrentDaoImpl()
	{
	}

	/**
	 * Method 'TrimesterCurrentDaoImpl'
	 * 
	 * @param userConn
	 */
	public TrimesterCurrentDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "tidsplan.trimester_current";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected TrimesterCurrent fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			TrimesterCurrent dto = new TrimesterCurrent();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected TrimesterCurrent[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			TrimesterCurrent dto = new TrimesterCurrent();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		TrimesterCurrent ret[] = new TrimesterCurrent[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(TrimesterCurrent dto, ResultSet rs) throws SQLException
	{
		dto.setIdTrimester( rs.getString( COLUMN_ID_TRIMESTER ) );
		dto.setStartDate( rs.getDate(COLUMN_START_DATE ) );
		dto.setEndDate( rs.getDate(COLUMN_END_DATE ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(TrimesterCurrent dto)
	{
	}

	/** 
	 * Returns all rows from the trimester_current table that match the specified arbitrary SQL statement
	 */
	public TrimesterCurrent[] findByDynamicSelect(String sql, Object[] sqlParams) throws TrimesterCurrentDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TrimesterCurrentDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the trimester_current table that match the specified arbitrary SQL statement
	 */
	public TrimesterCurrent[] findByDynamicWhere(String sql, Object[] sqlParams) throws TrimesterCurrentDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TrimesterCurrentDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
