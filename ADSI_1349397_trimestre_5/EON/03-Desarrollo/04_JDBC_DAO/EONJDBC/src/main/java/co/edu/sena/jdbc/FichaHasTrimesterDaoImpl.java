/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package co.edu.sena.jdbc;

import co.edu.sena.dao.*;
import co.edu.sena.factory.*;
import co.edu.sena.dto.*;
import co.edu.sena.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class FichaHasTrimesterDaoImpl extends AbstractDAO implements FichaHasTrimesterDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT number_Ficha, quarter_Name, acronyms_working_Day, speciality_Name FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( number_Ficha, quarter_Name, acronyms_working_Day, speciality_Name ) VALUES ( ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET number_Ficha = ?, quarter_Name = ?, acronyms_working_Day = ?, speciality_Name = ? WHERE number_Ficha = ? AND quarter_Name = ? AND acronyms_working_Day = ? AND speciality_Name = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE number_Ficha = ? AND quarter_Name = ? AND acronyms_working_Day = ? AND speciality_Name = ?";

	/** 
	 * Index of column number_Ficha
	 */
	protected static final int COLUMN_NUMBER_FICHA = 1;

	/** 
	 * Index of column quarter_Name
	 */
	protected static final int COLUMN_QUARTER_NAME = 2;

	/** 
	 * Index of column acronyms_working_Day
	 */
	protected static final int COLUMN_ACRONYMS_WORKING_DAY = 3;

	/** 
	 * Index of column speciality_Name
	 */
	protected static final int COLUMN_SPECIALITY_NAME = 4;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 4;

	/** 
	 * Index of primary-key column number_Ficha
	 */
	protected static final int PK_COLUMN_NUMBER_FICHA = 1;

	/** 
	 * Index of primary-key column quarter_Name
	 */
	protected static final int PK_COLUMN_QUARTER_NAME = 2;

	/** 
	 * Index of primary-key column acronyms_working_Day
	 */
	protected static final int PK_COLUMN_ACRONYMS_WORKING_DAY = 3;

	/** 
	 * Index of primary-key column speciality_Name
	 */
	protected static final int PK_COLUMN_SPECIALITY_NAME = 4;

	/** 
	 * Inserts a new row in the ficha_has_trimester table.
	 */
	public FichaHasTrimesterPk insert(FichaHasTrimester dto) throws FichaHasTrimesterDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setString( index++, dto.getNumberFicha() );
			stmt.setString( index++, dto.getQuarterName() );
			stmt.setString( index++, dto.getAcronymsWorkingDay() );
			stmt.setString( index++, dto.getSpecialityName() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new FichaHasTrimesterDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the ficha_has_trimester table.
	 */
	public void update(FichaHasTrimesterPk pk, FichaHasTrimester dto) throws FichaHasTrimesterDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setString( index++, dto.getNumberFicha() );
			stmt.setString( index++, dto.getQuarterName() );
			stmt.setString( index++, dto.getAcronymsWorkingDay() );
			stmt.setString( index++, dto.getSpecialityName() );
			stmt.setString( 5, pk.getNumberFicha() );
			stmt.setString( 6, pk.getQuarterName() );
			stmt.setString( 7, pk.getAcronymsWorkingDay() );
			stmt.setString( 8, pk.getSpecialityName() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new FichaHasTrimesterDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the ficha_has_trimester table.
	 */
	public void delete(FichaHasTrimesterPk pk) throws FichaHasTrimesterDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setString( 1, pk.getNumberFicha() );
			stmt.setString( 2, pk.getQuarterName() );
			stmt.setString( 3, pk.getAcronymsWorkingDay() );
			stmt.setString( 4, pk.getSpecialityName() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new FichaHasTrimesterDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the ficha_has_trimester table that matches the specified primary-key value.
	 */
	public FichaHasTrimester findByPrimaryKey(FichaHasTrimesterPk pk) throws FichaHasTrimesterDaoException
	{
		return findByPrimaryKey( pk.getNumberFicha(), pk.getQuarterName(), pk.getAcronymsWorkingDay(), pk.getSpecialityName() );
	}

	/** 
	 * Returns all rows from the ficha_has_trimester table that match the criteria 'number_Ficha = :numberFicha AND quarter_Name = :quarterName AND acronyms_working_Day = :acronymsWorkingDay AND speciality_Name = :specialityName'.
	 */
	public FichaHasTrimester findByPrimaryKey(String numberFicha, String quarterName, String acronymsWorkingDay, String specialityName) throws FichaHasTrimesterDaoException
	{
		FichaHasTrimester ret[] = findByDynamicSelect( SQL_SELECT + " WHERE number_Ficha = ? AND quarter_Name = ? AND acronyms_working_Day = ? AND speciality_Name = ?", new Object[] { numberFicha, quarterName, acronymsWorkingDay, specialityName } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the ficha_has_trimester table that match the criteria ''.
	 */
	public FichaHasTrimester[] findAll() throws FichaHasTrimesterDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY number_Ficha, quarter_Name, acronyms_working_Day, speciality_Name", null );
	}

	/** 
	 * Returns all rows from the ficha_has_trimester table that match the criteria 'number_Ficha = :numberFicha'.
	 */
	public FichaHasTrimester[] findByFicha(String numberFicha) throws FichaHasTrimesterDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE number_Ficha = ?", new Object[] { numberFicha } );
	}

	/** 
	 * Returns all rows from the ficha_has_trimester table that match the criteria 'quarter_Name = :quarterName AND acronyms_working_Day = :acronymsWorkingDay AND speciality_Name = :specialityName'.
	 */
	public FichaHasTrimester[] findByTrimester(String quarterName, String acronymsWorkingDay, String specialityName) throws FichaHasTrimesterDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE quarter_Name = ? AND acronyms_working_Day = ? AND speciality_Name = ?", new Object[] { quarterName, acronymsWorkingDay, specialityName } );
	}

	/** 
	 * Returns all rows from the ficha_has_trimester table that match the criteria 'number_Ficha = :numberFicha'.
	 */
	public FichaHasTrimester[] findWhereNumberFichaEquals(String numberFicha) throws FichaHasTrimesterDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE number_Ficha = ? ORDER BY number_Ficha", new Object[] { numberFicha } );
	}

	/** 
	 * Returns all rows from the ficha_has_trimester table that match the criteria 'quarter_Name = :quarterName'.
	 */
	public FichaHasTrimester[] findWhereQuarterNameEquals(String quarterName) throws FichaHasTrimesterDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE quarter_Name = ? ORDER BY quarter_Name", new Object[] { quarterName } );
	}

	/** 
	 * Returns all rows from the ficha_has_trimester table that match the criteria 'acronyms_working_Day = :acronymsWorkingDay'.
	 */
	public FichaHasTrimester[] findWhereAcronymsWorkingDayEquals(String acronymsWorkingDay) throws FichaHasTrimesterDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE acronyms_working_Day = ? ORDER BY acronyms_working_Day", new Object[] { acronymsWorkingDay } );
	}

	/** 
	 * Returns all rows from the ficha_has_trimester table that match the criteria 'speciality_Name = :specialityName'.
	 */
	public FichaHasTrimester[] findWhereSpecialityNameEquals(String specialityName) throws FichaHasTrimesterDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE speciality_Name = ? ORDER BY speciality_Name", new Object[] { specialityName } );
	}

	/**
	 * Method 'FichaHasTrimesterDaoImpl'
	 * 
	 */
	public FichaHasTrimesterDaoImpl()
	{
	}

	/**
	 * Method 'FichaHasTrimesterDaoImpl'
	 * 
	 * @param userConn
	 */
	public FichaHasTrimesterDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "eon.ficha_has_trimester";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected FichaHasTrimester fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			FichaHasTrimester dto = new FichaHasTrimester();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected FichaHasTrimester[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			FichaHasTrimester dto = new FichaHasTrimester();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		FichaHasTrimester ret[] = new FichaHasTrimester[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(FichaHasTrimester dto, ResultSet rs) throws SQLException
	{
		dto.setNumberFicha( rs.getString( COLUMN_NUMBER_FICHA ) );
		dto.setQuarterName( rs.getString( COLUMN_QUARTER_NAME ) );
		dto.setAcronymsWorkingDay( rs.getString( COLUMN_ACRONYMS_WORKING_DAY ) );
		dto.setSpecialityName( rs.getString( COLUMN_SPECIALITY_NAME ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(FichaHasTrimester dto)
	{
	}

	/** 
	 * Returns all rows from the ficha_has_trimester table that match the specified arbitrary SQL statement
	 */
	public FichaHasTrimester[] findByDynamicSelect(String sql, Object[] sqlParams) throws FichaHasTrimesterDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new FichaHasTrimesterDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the ficha_has_trimester table that match the specified arbitrary SQL statement
	 */
	public FichaHasTrimester[] findByDynamicWhere(String sql, Object[] sqlParams) throws FichaHasTrimesterDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new FichaHasTrimesterDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
