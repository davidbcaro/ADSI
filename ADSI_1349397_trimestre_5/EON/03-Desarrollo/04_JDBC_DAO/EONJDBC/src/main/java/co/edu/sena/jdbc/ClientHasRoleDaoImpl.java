/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package co.edu.sena.jdbc;

import co.edu.sena.dao.*;
import co.edu.sena.factory.*;
import co.edu.sena.dto.*;
import co.edu.sena.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class ClientHasRoleDaoImpl extends AbstractDAO implements ClientHasRoleDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT document_Number, id_Document, id_Role, Status FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( document_Number, id_Document, id_Role, Status ) VALUES ( ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET document_Number = ?, id_Document = ?, id_Role = ?, Status = ? WHERE document_Number = ? AND id_Document = ? AND id_Role = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE document_Number = ? AND id_Document = ? AND id_Role = ?";

	/** 
	 * Index of column document_Number
	 */
	protected static final int COLUMN_DOCUMENT_NUMBER = 1;

	/** 
	 * Index of column id_Document
	 */
	protected static final int COLUMN_ID_DOCUMENT = 2;

	/** 
	 * Index of column id_Role
	 */
	protected static final int COLUMN_ID_ROLE = 3;

	/** 
	 * Index of column Status
	 */
	protected static final int COLUMN_STATUS = 4;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 4;

	/** 
	 * Index of primary-key column document_Number
	 */
	protected static final int PK_COLUMN_DOCUMENT_NUMBER = 1;

	/** 
	 * Index of primary-key column id_Document
	 */
	protected static final int PK_COLUMN_ID_DOCUMENT = 2;

	/** 
	 * Index of primary-key column id_Role
	 */
	protected static final int PK_COLUMN_ID_ROLE = 3;

	/** 
	 * Inserts a new row in the client_has_role table.
	 */
	public ClientHasRolePk insert(ClientHasRole dto) throws ClientHasRoleDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setString( index++, dto.getDocumentNumber() );
			stmt.setString( index++, dto.getIdDocument() );
			stmt.setString( index++, dto.getIdRole() );
			if (dto.isStatusNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setShort( index++, dto.getStatus() );
			}
		
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ClientHasRoleDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the client_has_role table.
	 */
	public void update(ClientHasRolePk pk, ClientHasRole dto) throws ClientHasRoleDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setString( index++, dto.getDocumentNumber() );
			stmt.setString( index++, dto.getIdDocument() );
			stmt.setString( index++, dto.getIdRole() );
			if (dto.isStatusNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setShort( index++, dto.getStatus() );
			}
		
			stmt.setString( 5, pk.getDocumentNumber() );
			stmt.setString( 6, pk.getIdDocument() );
			stmt.setString( 7, pk.getIdRole() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ClientHasRoleDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the client_has_role table.
	 */
	public void delete(ClientHasRolePk pk) throws ClientHasRoleDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setString( 1, pk.getDocumentNumber() );
			stmt.setString( 2, pk.getIdDocument() );
			stmt.setString( 3, pk.getIdRole() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ClientHasRoleDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the client_has_role table that matches the specified primary-key value.
	 */
	public ClientHasRole findByPrimaryKey(ClientHasRolePk pk) throws ClientHasRoleDaoException
	{
		return findByPrimaryKey( pk.getDocumentNumber(), pk.getIdDocument(), pk.getIdRole() );
	}

	/** 
	 * Returns all rows from the client_has_role table that match the criteria 'document_Number = :documentNumber AND id_Document = :idDocument AND id_Role = :idRole'.
	 */
	public ClientHasRole findByPrimaryKey(String documentNumber, String idDocument, String idRole) throws ClientHasRoleDaoException
	{
		ClientHasRole ret[] = findByDynamicSelect( SQL_SELECT + " WHERE document_Number = ? AND id_Document = ? AND id_Role = ?", new Object[] { documentNumber, idDocument, idRole } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the client_has_role table that match the criteria ''.
	 */
	public ClientHasRole[] findAll() throws ClientHasRoleDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY document_Number, id_Document, id_Role", null );
	}

	/** 
	 * Returns all rows from the client_has_role table that match the criteria 'document_Number = :documentNumber AND id_Document = :idDocument'.
	 */
	public ClientHasRole[] findByClient(String documentNumber, String idDocument) throws ClientHasRoleDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE document_Number = ? AND id_Document = ?", new Object[] { documentNumber, idDocument } );
	}

	/** 
	 * Returns all rows from the client_has_role table that match the criteria 'id_Role = :idRole'.
	 */
	public ClientHasRole[] findByRole(String idRole) throws ClientHasRoleDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE id_Role = ?", new Object[] { idRole } );
	}

	/** 
	 * Returns all rows from the client_has_role table that match the criteria 'document_Number = :documentNumber'.
	 */
	public ClientHasRole[] findWhereDocumentNumberEquals(String documentNumber) throws ClientHasRoleDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE document_Number = ? ORDER BY document_Number", new Object[] { documentNumber } );
	}

	/** 
	 * Returns all rows from the client_has_role table that match the criteria 'id_Document = :idDocument'.
	 */
	public ClientHasRole[] findWhereIdDocumentEquals(String idDocument) throws ClientHasRoleDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE id_Document = ? ORDER BY id_Document", new Object[] { idDocument } );
	}

	/** 
	 * Returns all rows from the client_has_role table that match the criteria 'id_Role = :idRole'.
	 */
	public ClientHasRole[] findWhereIdRoleEquals(String idRole) throws ClientHasRoleDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE id_Role = ? ORDER BY id_Role", new Object[] { idRole } );
	}

	/** 
	 * Returns all rows from the client_has_role table that match the criteria 'Status = :status'.
	 */
	public ClientHasRole[] findWhereStatusEquals(short status) throws ClientHasRoleDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE Status = ? ORDER BY Status", new Object[] {  new Short(status) } );
	}

	/**
	 * Method 'ClientHasRoleDaoImpl'
	 * 
	 */
	public ClientHasRoleDaoImpl()
	{
	}

	/**
	 * Method 'ClientHasRoleDaoImpl'
	 * 
	 * @param userConn
	 */
	public ClientHasRoleDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "eon.client_has_role";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected ClientHasRole fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			ClientHasRole dto = new ClientHasRole();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected ClientHasRole[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			ClientHasRole dto = new ClientHasRole();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		ClientHasRole ret[] = new ClientHasRole[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(ClientHasRole dto, ResultSet rs) throws SQLException
	{
		dto.setDocumentNumber( rs.getString( COLUMN_DOCUMENT_NUMBER ) );
		dto.setIdDocument( rs.getString( COLUMN_ID_DOCUMENT ) );
		dto.setIdRole( rs.getString( COLUMN_ID_ROLE ) );
		dto.setStatus( rs.getShort( COLUMN_STATUS ) );
		if (rs.wasNull()) {
			dto.setStatusNull( true );
		}
		
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(ClientHasRole dto)
	{
	}

	/** 
	 * Returns all rows from the client_has_role table that match the specified arbitrary SQL statement
	 */
	public ClientHasRole[] findByDynamicSelect(String sql, Object[] sqlParams) throws ClientHasRoleDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ClientHasRoleDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the client_has_role table that match the specified arbitrary SQL statement
	 */
	public ClientHasRole[] findByDynamicWhere(String sql, Object[] sqlParams) throws ClientHasRoleDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ClientHasRoleDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
