/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package co.edu.sena.jdbc;

import co.edu.sena.dao.*;
import co.edu.sena.factory.*;
import co.edu.sena.dto.*;
import co.edu.sena.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class TrimesterDaoImpl extends AbstractDAO implements TrimesterDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT quarter_Name, acronyms_Working_Day, speciality_Name FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( quarter_Name, acronyms_Working_Day, speciality_Name ) VALUES ( ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET quarter_Name = ?, acronyms_Working_Day = ?, speciality_Name = ? WHERE quarter_Name = ? AND acronyms_Working_Day = ? AND speciality_Name = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE quarter_Name = ? AND acronyms_Working_Day = ? AND speciality_Name = ?";

	/** 
	 * Index of column quarter_Name
	 */
	protected static final int COLUMN_QUARTER_NAME = 1;

	/** 
	 * Index of column acronyms_Working_Day
	 */
	protected static final int COLUMN_ACRONYMS_WORKING_DAY = 2;

	/** 
	 * Index of column speciality_Name
	 */
	protected static final int COLUMN_SPECIALITY_NAME = 3;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 3;

	/** 
	 * Index of primary-key column quarter_Name
	 */
	protected static final int PK_COLUMN_QUARTER_NAME = 1;

	/** 
	 * Index of primary-key column acronyms_Working_Day
	 */
	protected static final int PK_COLUMN_ACRONYMS_WORKING_DAY = 2;

	/** 
	 * Index of primary-key column speciality_Name
	 */
	protected static final int PK_COLUMN_SPECIALITY_NAME = 3;

	/** 
	 * Inserts a new row in the trimester table.
	 */
	public TrimesterPk insert(Trimester dto) throws TrimesterDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setString( index++, dto.getQuarterName() );
			stmt.setString( index++, dto.getAcronymsWorkingDay() );
			stmt.setString( index++, dto.getSpecialityName() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TrimesterDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the trimester table.
	 */
	public void update(TrimesterPk pk, Trimester dto) throws TrimesterDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setString( index++, dto.getQuarterName() );
			stmt.setString( index++, dto.getAcronymsWorkingDay() );
			stmt.setString( index++, dto.getSpecialityName() );
			stmt.setString( 4, pk.getQuarterName() );
			stmt.setString( 5, pk.getAcronymsWorkingDay() );
			stmt.setString( 6, pk.getSpecialityName() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TrimesterDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the trimester table.
	 */
	public void delete(TrimesterPk pk) throws TrimesterDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setString( 1, pk.getQuarterName() );
			stmt.setString( 2, pk.getAcronymsWorkingDay() );
			stmt.setString( 3, pk.getSpecialityName() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TrimesterDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the trimester table that matches the specified primary-key value.
	 */
	public Trimester findByPrimaryKey(TrimesterPk pk) throws TrimesterDaoException
	{
		return findByPrimaryKey( pk.getQuarterName(), pk.getAcronymsWorkingDay(), pk.getSpecialityName() );
	}

	/** 
	 * Returns all rows from the trimester table that match the criteria 'quarter_Name = :quarterName AND acronyms_Working_Day = :acronymsWorkingDay AND speciality_Name = :specialityName'.
	 */
	public Trimester findByPrimaryKey(String quarterName, String acronymsWorkingDay, String specialityName) throws TrimesterDaoException
	{
		Trimester ret[] = findByDynamicSelect( SQL_SELECT + " WHERE quarter_Name = ? AND acronyms_Working_Day = ? AND speciality_Name = ?", new Object[] { quarterName, acronymsWorkingDay, specialityName } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the trimester table that match the criteria ''.
	 */
	public Trimester[] findAll() throws TrimesterDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY quarter_Name, acronyms_Working_Day, speciality_Name", null );
	}

	/** 
	 * Returns all rows from the trimester table that match the criteria 'speciality_Name = :specialityName'.
	 */
	public Trimester[] findBySpeciality(String specialityName) throws TrimesterDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE speciality_Name = ?", new Object[] { specialityName } );
	}

	/** 
	 * Returns all rows from the trimester table that match the criteria 'acronyms_Working_Day = :acronymsWorkingDay'.
	 */
	public Trimester[] findByWorkingDay(String acronymsWorkingDay) throws TrimesterDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE acronyms_Working_Day = ?", new Object[] { acronymsWorkingDay } );
	}

	/** 
	 * Returns all rows from the trimester table that match the criteria 'quarter_Name = :quarterName'.
	 */
	public Trimester[] findWhereQuarterNameEquals(String quarterName) throws TrimesterDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE quarter_Name = ? ORDER BY quarter_Name", new Object[] { quarterName } );
	}

	/** 
	 * Returns all rows from the trimester table that match the criteria 'acronyms_Working_Day = :acronymsWorkingDay'.
	 */
	public Trimester[] findWhereAcronymsWorkingDayEquals(String acronymsWorkingDay) throws TrimesterDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE acronyms_Working_Day = ? ORDER BY acronyms_Working_Day", new Object[] { acronymsWorkingDay } );
	}

	/** 
	 * Returns all rows from the trimester table that match the criteria 'speciality_Name = :specialityName'.
	 */
	public Trimester[] findWhereSpecialityNameEquals(String specialityName) throws TrimesterDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE speciality_Name = ? ORDER BY speciality_Name", new Object[] { specialityName } );
	}

	/**
	 * Method 'TrimesterDaoImpl'
	 * 
	 */
	public TrimesterDaoImpl()
	{
	}

	/**
	 * Method 'TrimesterDaoImpl'
	 * 
	 * @param userConn
	 */
	public TrimesterDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "eon.trimester";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected Trimester fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			Trimester dto = new Trimester();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected Trimester[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			Trimester dto = new Trimester();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		Trimester ret[] = new Trimester[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(Trimester dto, ResultSet rs) throws SQLException
	{
		dto.setQuarterName( rs.getString( COLUMN_QUARTER_NAME ) );
		dto.setAcronymsWorkingDay( rs.getString( COLUMN_ACRONYMS_WORKING_DAY ) );
		dto.setSpecialityName( rs.getString( COLUMN_SPECIALITY_NAME ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(Trimester dto)
	{
	}

	/** 
	 * Returns all rows from the trimester table that match the specified arbitrary SQL statement
	 */
	public Trimester[] findByDynamicSelect(String sql, Object[] sqlParams) throws TrimesterDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TrimesterDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the trimester table that match the specified arbitrary SQL statement
	 */
	public Trimester[] findByDynamicWhere(String sql, Object[] sqlParams) throws TrimesterDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new TrimesterDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
