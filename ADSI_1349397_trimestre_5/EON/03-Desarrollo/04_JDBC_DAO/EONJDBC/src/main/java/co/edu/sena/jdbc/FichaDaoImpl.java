/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package co.edu.sena.jdbc;

import co.edu.sena.dao.*;
import co.edu.sena.factory.*;
import java.util.Date;
import co.edu.sena.dto.*;
import co.edu.sena.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class FichaDaoImpl extends AbstractDAO implements FichaDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT number_Ficha, start_Date, end_Date, Route, Program_Code, Version, name_State FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( number_Ficha, start_Date, end_Date, Route, Program_Code, Version, name_State ) VALUES ( ?, ?, ?, ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET number_Ficha = ?, start_Date = ?, end_Date = ?, Route = ?, Program_Code = ?, Version = ?, name_State = ? WHERE number_Ficha = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE number_Ficha = ?";

	/** 
	 * Index of column number_Ficha
	 */
	protected static final int COLUMN_NUMBER_FICHA = 1;

	/** 
	 * Index of column start_Date
	 */
	protected static final int COLUMN_START_DATE = 2;

	/** 
	 * Index of column end_Date
	 */
	protected static final int COLUMN_END_DATE = 3;

	/** 
	 * Index of column Route
	 */
	protected static final int COLUMN_ROUTE = 4;

	/** 
	 * Index of column Program_Code
	 */
	protected static final int COLUMN_PROGRAM_CODE = 5;

	/** 
	 * Index of column Version
	 */
	protected static final int COLUMN_VERSION = 6;

	/** 
	 * Index of column name_State
	 */
	protected static final int COLUMN_NAME_STATE = 7;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 7;

	/** 
	 * Index of primary-key column number_Ficha
	 */
	protected static final int PK_COLUMN_NUMBER_FICHA = 1;

	/** 
	 * Inserts a new row in the ficha table.
	 */
	public FichaPk insert(Ficha dto) throws FichaDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setString( index++, dto.getNumberFicha() );
			stmt.setDate(index++, dto.getStartDate()==null ? null : new java.sql.Date( dto.getStartDate().getTime() ) );
			stmt.setDate(index++, dto.getEndDate()==null ? null : new java.sql.Date( dto.getEndDate().getTime() ) );
			stmt.setString( index++, dto.getRoute() );
			stmt.setString( index++, dto.getProgramCode() );
			stmt.setString( index++, dto.getVersion() );
			stmt.setString( index++, dto.getNameState() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new FichaDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the ficha table.
	 */
	public void update(FichaPk pk, Ficha dto) throws FichaDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setString( index++, dto.getNumberFicha() );
			stmt.setDate(index++, dto.getStartDate()==null ? null : new java.sql.Date( dto.getStartDate().getTime() ) );
			stmt.setDate(index++, dto.getEndDate()==null ? null : new java.sql.Date( dto.getEndDate().getTime() ) );
			stmt.setString( index++, dto.getRoute() );
			stmt.setString( index++, dto.getProgramCode() );
			stmt.setString( index++, dto.getVersion() );
			stmt.setString( index++, dto.getNameState() );
			stmt.setString( 8, pk.getNumberFicha() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new FichaDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the ficha table.
	 */
	public void delete(FichaPk pk) throws FichaDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setString( 1, pk.getNumberFicha() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new FichaDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the ficha table that matches the specified primary-key value.
	 */
	public Ficha findByPrimaryKey(FichaPk pk) throws FichaDaoException
	{
		return findByPrimaryKey( pk.getNumberFicha() );
	}

	/** 
	 * Returns all rows from the ficha table that match the criteria 'number_Ficha = :numberFicha'.
	 */
	public Ficha findByPrimaryKey(String numberFicha) throws FichaDaoException
	{
		Ficha ret[] = findByDynamicSelect( SQL_SELECT + " WHERE number_Ficha = ?", new Object[] { numberFicha } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the ficha table that match the criteria ''.
	 */
	public Ficha[] findAll() throws FichaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY number_Ficha", null );
	}

	/** 
	 * Returns all rows from the ficha table that match the criteria 'Program_Code = :programCode AND Version = :version'.
	 */
	public Ficha[] findByProgram(String programCode, String version) throws FichaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE Program_Code = ? AND Version = ?", new Object[] { programCode, version } );
	}

	/** 
	 * Returns all rows from the ficha table that match the criteria 'name_State = :nameState'.
	 */
	public Ficha[] findByStatusFicha(String nameState) throws FichaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE name_State = ?", new Object[] { nameState } );
	}

	/** 
	 * Returns all rows from the ficha table that match the criteria 'number_Ficha = :numberFicha'.
	 */
	public Ficha[] findWhereNumberFichaEquals(String numberFicha) throws FichaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE number_Ficha = ? ORDER BY number_Ficha", new Object[] { numberFicha } );
	}

	/** 
	 * Returns all rows from the ficha table that match the criteria 'start_Date = :startDate'.
	 */
	public Ficha[] findWhereStartDateEquals(Date startDate) throws FichaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE start_Date = ? ORDER BY start_Date", new Object[] { startDate==null ? null : new java.sql.Date( startDate.getTime() ) } );
	}

	/** 
	 * Returns all rows from the ficha table that match the criteria 'end_Date = :endDate'.
	 */
	public Ficha[] findWhereEndDateEquals(Date endDate) throws FichaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE end_Date = ? ORDER BY end_Date", new Object[] { endDate==null ? null : new java.sql.Date( endDate.getTime() ) } );
	}

	/** 
	 * Returns all rows from the ficha table that match the criteria 'Route = :route'.
	 */
	public Ficha[] findWhereRouteEquals(String route) throws FichaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE Route = ? ORDER BY Route", new Object[] { route } );
	}

	/** 
	 * Returns all rows from the ficha table that match the criteria 'Program_Code = :programCode'.
	 */
	public Ficha[] findWhereProgramCodeEquals(String programCode) throws FichaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE Program_Code = ? ORDER BY Program_Code", new Object[] { programCode } );
	}

	/** 
	 * Returns all rows from the ficha table that match the criteria 'Version = :version'.
	 */
	public Ficha[] findWhereVersionEquals(String version) throws FichaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE Version = ? ORDER BY Version", new Object[] { version } );
	}

	/** 
	 * Returns all rows from the ficha table that match the criteria 'name_State = :nameState'.
	 */
	public Ficha[] findWhereNameStateEquals(String nameState) throws FichaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE name_State = ? ORDER BY name_State", new Object[] { nameState } );
	}

	/**
	 * Method 'FichaDaoImpl'
	 * 
	 */
	public FichaDaoImpl()
	{
	}

	/**
	 * Method 'FichaDaoImpl'
	 * 
	 * @param userConn
	 */
	public FichaDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "eon.ficha";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected Ficha fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			Ficha dto = new Ficha();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected Ficha[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			Ficha dto = new Ficha();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		Ficha ret[] = new Ficha[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(Ficha dto, ResultSet rs) throws SQLException
	{
		dto.setNumberFicha( rs.getString( COLUMN_NUMBER_FICHA ) );
		dto.setStartDate( rs.getDate(COLUMN_START_DATE ) );
		dto.setEndDate( rs.getDate(COLUMN_END_DATE ) );
		dto.setRoute( rs.getString( COLUMN_ROUTE ) );
		dto.setProgramCode( rs.getString( COLUMN_PROGRAM_CODE ) );
		dto.setVersion( rs.getString( COLUMN_VERSION ) );
		dto.setNameState( rs.getString( COLUMN_NAME_STATE ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(Ficha dto)
	{
	}

	/** 
	 * Returns all rows from the ficha table that match the specified arbitrary SQL statement
	 */
	public Ficha[] findByDynamicSelect(String sql, Object[] sqlParams) throws FichaDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new FichaDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the ficha table that match the specified arbitrary SQL statement
	 */
	public Ficha[] findByDynamicWhere(String sql, Object[] sqlParams) throws FichaDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new FichaDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
