/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package co.edu.sena.jdbc;

import co.edu.sena.dao.*;
import co.edu.sena.factory.*;
import co.edu.sena.dto.*;
import co.edu.sena.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class ViewedResultsDaoImpl extends AbstractDAO implements ViewedResultsDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT number_Ficha, quarter_Name, acronyms_Working_Day, speciality_Name, Learning_Result_Code, Program_Code, Competition_Code, Version FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( number_Ficha, quarter_Name, acronyms_Working_Day, speciality_Name, Learning_Result_Code, Program_Code, Competition_Code, Version ) VALUES ( ?, ?, ?, ?, ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET number_Ficha = ?, quarter_Name = ?, acronyms_Working_Day = ?, speciality_Name = ?, Learning_Result_Code = ?, Program_Code = ?, Competition_Code = ?, Version = ? WHERE number_Ficha = ? AND quarter_Name = ? AND acronyms_Working_Day = ? AND speciality_Name = ? AND Learning_Result_Code = ? AND Program_Code = ? AND Version = ? AND Competition_Code = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE number_Ficha = ? AND quarter_Name = ? AND acronyms_Working_Day = ? AND speciality_Name = ? AND Learning_Result_Code = ? AND Program_Code = ? AND Version = ? AND Competition_Code = ?";

	/** 
	 * Index of column number_Ficha
	 */
	protected static final int COLUMN_NUMBER_FICHA = 1;

	/** 
	 * Index of column quarter_Name
	 */
	protected static final int COLUMN_QUARTER_NAME = 2;

	/** 
	 * Index of column acronyms_Working_Day
	 */
	protected static final int COLUMN_ACRONYMS_WORKING_DAY = 3;

	/** 
	 * Index of column speciality_Name
	 */
	protected static final int COLUMN_SPECIALITY_NAME = 4;

	/** 
	 * Index of column Learning_Result_Code
	 */
	protected static final int COLUMN_LEARNING_RESULT_CODE = 5;

	/** 
	 * Index of column Program_Code
	 */
	protected static final int COLUMN_PROGRAM_CODE = 6;

	/** 
	 * Index of column Competition_Code
	 */
	protected static final int COLUMN_COMPETITION_CODE = 7;

	/** 
	 * Index of column Version
	 */
	protected static final int COLUMN_VERSION = 8;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 8;

	/** 
	 * Index of primary-key column number_Ficha
	 */
	protected static final int PK_COLUMN_NUMBER_FICHA = 1;

	/** 
	 * Index of primary-key column quarter_Name
	 */
	protected static final int PK_COLUMN_QUARTER_NAME = 2;

	/** 
	 * Index of primary-key column acronyms_Working_Day
	 */
	protected static final int PK_COLUMN_ACRONYMS_WORKING_DAY = 3;

	/** 
	 * Index of primary-key column speciality_Name
	 */
	protected static final int PK_COLUMN_SPECIALITY_NAME = 4;

	/** 
	 * Index of primary-key column Learning_Result_Code
	 */
	protected static final int PK_COLUMN_LEARNING_RESULT_CODE = 5;

	/** 
	 * Index of primary-key column Program_Code
	 */
	protected static final int PK_COLUMN_PROGRAM_CODE = 6;

	/** 
	 * Index of primary-key column Version
	 */
	protected static final int PK_COLUMN_VERSION = 7;

	/** 
	 * Index of primary-key column Competition_Code
	 */
	protected static final int PK_COLUMN_COMPETITION_CODE = 8;

	/** 
	 * Inserts a new row in the viewed_results table.
	 */
	public ViewedResultsPk insert(ViewedResults dto) throws ViewedResultsDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setString( index++, dto.getNumberFicha() );
			stmt.setString( index++, dto.getQuarterName() );
			stmt.setString( index++, dto.getAcronymsWorkingDay() );
			stmt.setString( index++, dto.getSpecialityName() );
			stmt.setString( index++, dto.getLearningResultCode() );
			stmt.setString( index++, dto.getProgramCode() );
			stmt.setString( index++, dto.getCompetitionCode() );
			stmt.setString( index++, dto.getVersion() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ViewedResultsDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the viewed_results table.
	 */
	public void update(ViewedResultsPk pk, ViewedResults dto) throws ViewedResultsDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setString( index++, dto.getNumberFicha() );
			stmt.setString( index++, dto.getQuarterName() );
			stmt.setString( index++, dto.getAcronymsWorkingDay() );
			stmt.setString( index++, dto.getSpecialityName() );
			stmt.setString( index++, dto.getLearningResultCode() );
			stmt.setString( index++, dto.getProgramCode() );
			stmt.setString( index++, dto.getCompetitionCode() );
			stmt.setString( index++, dto.getVersion() );
			stmt.setString( 9, pk.getNumberFicha() );
			stmt.setString( 10, pk.getQuarterName() );
			stmt.setString( 11, pk.getAcronymsWorkingDay() );
			stmt.setString( 12, pk.getSpecialityName() );
			stmt.setString( 13, pk.getLearningResultCode() );
			stmt.setString( 14, pk.getProgramCode() );
			stmt.setString( 15, pk.getVersion() );
			stmt.setString( 16, pk.getCompetitionCode() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ViewedResultsDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the viewed_results table.
	 */
	public void delete(ViewedResultsPk pk) throws ViewedResultsDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setString( 1, pk.getNumberFicha() );
			stmt.setString( 2, pk.getQuarterName() );
			stmt.setString( 3, pk.getAcronymsWorkingDay() );
			stmt.setString( 4, pk.getSpecialityName() );
			stmt.setString( 5, pk.getLearningResultCode() );
			stmt.setString( 6, pk.getProgramCode() );
			stmt.setString( 7, pk.getVersion() );
			stmt.setString( 8, pk.getCompetitionCode() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ViewedResultsDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the viewed_results table that matches the specified primary-key value.
	 */
	public ViewedResults findByPrimaryKey(ViewedResultsPk pk) throws ViewedResultsDaoException
	{
		return findByPrimaryKey( pk.getNumberFicha(), pk.getQuarterName(), pk.getAcronymsWorkingDay(), pk.getSpecialityName(), pk.getLearningResultCode(), pk.getProgramCode(), pk.getVersion(), pk.getCompetitionCode() );
	}

	/** 
	 * Returns all rows from the viewed_results table that match the criteria 'number_Ficha = :numberFicha AND quarter_Name = :quarterName AND acronyms_Working_Day = :acronymsWorkingDay AND speciality_Name = :specialityName AND Learning_Result_Code = :learningResultCode AND Program_Code = :programCode AND Version = :version AND Competition_Code = :competitionCode'.
	 */
	public ViewedResults findByPrimaryKey(String numberFicha, String quarterName, String acronymsWorkingDay, String specialityName, String learningResultCode, String programCode, String version, String competitionCode) throws ViewedResultsDaoException
	{
		ViewedResults ret[] = findByDynamicSelect( SQL_SELECT + " WHERE number_Ficha = ? AND quarter_Name = ? AND acronyms_Working_Day = ? AND speciality_Name = ? AND Learning_Result_Code = ? AND Program_Code = ? AND Version = ? AND Competition_Code = ?", new Object[] { numberFicha, quarterName, acronymsWorkingDay, specialityName, learningResultCode, programCode, version, competitionCode } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the viewed_results table that match the criteria ''.
	 */
	public ViewedResults[] findAll() throws ViewedResultsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY number_Ficha, quarter_Name, acronyms_Working_Day, speciality_Name, Learning_Result_Code, Program_Code, Version, Competition_Code", null );
	}

	/** 
	 * Returns all rows from the viewed_results table that match the criteria 'number_Ficha = :numberFicha AND quarter_Name = :quarterName AND acronyms_Working_Day = :acronymsWorkingDay AND speciality_Name = :specialityName'.
	 */
	public ViewedResults[] findByFichaHasTrimester(String numberFicha, String quarterName, String acronymsWorkingDay, String specialityName) throws ViewedResultsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE number_Ficha = ? AND quarter_Name = ? AND acronyms_Working_Day = ? AND speciality_Name = ?", new Object[] { numberFicha, quarterName, acronymsWorkingDay, specialityName } );
	}

	/** 
	 * Returns all rows from the viewed_results table that match the criteria 'Learning_Result_Code = :learningResultCode AND Program_Code = :programCode AND Version = :version AND Competition_Code = :competitionCode'.
	 */
	public ViewedResults[] findByLearningResult(String learningResultCode, String programCode, String version, String competitionCode) throws ViewedResultsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE Learning_Result_Code = ? AND Program_Code = ? AND Version = ? AND Competition_Code = ?", new Object[] { learningResultCode, programCode, version, competitionCode } );
	}

	/** 
	 * Returns all rows from the viewed_results table that match the criteria 'number_Ficha = :numberFicha'.
	 */
	public ViewedResults[] findWhereNumberFichaEquals(String numberFicha) throws ViewedResultsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE number_Ficha = ? ORDER BY number_Ficha", new Object[] { numberFicha } );
	}

	/** 
	 * Returns all rows from the viewed_results table that match the criteria 'quarter_Name = :quarterName'.
	 */
	public ViewedResults[] findWhereQuarterNameEquals(String quarterName) throws ViewedResultsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE quarter_Name = ? ORDER BY quarter_Name", new Object[] { quarterName } );
	}

	/** 
	 * Returns all rows from the viewed_results table that match the criteria 'acronyms_Working_Day = :acronymsWorkingDay'.
	 */
	public ViewedResults[] findWhereAcronymsWorkingDayEquals(String acronymsWorkingDay) throws ViewedResultsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE acronyms_Working_Day = ? ORDER BY acronyms_Working_Day", new Object[] { acronymsWorkingDay } );
	}

	/** 
	 * Returns all rows from the viewed_results table that match the criteria 'speciality_Name = :specialityName'.
	 */
	public ViewedResults[] findWhereSpecialityNameEquals(String specialityName) throws ViewedResultsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE speciality_Name = ? ORDER BY speciality_Name", new Object[] { specialityName } );
	}

	/** 
	 * Returns all rows from the viewed_results table that match the criteria 'Learning_Result_Code = :learningResultCode'.
	 */
	public ViewedResults[] findWhereLearningResultCodeEquals(String learningResultCode) throws ViewedResultsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE Learning_Result_Code = ? ORDER BY Learning_Result_Code", new Object[] { learningResultCode } );
	}

	/** 
	 * Returns all rows from the viewed_results table that match the criteria 'Program_Code = :programCode'.
	 */
	public ViewedResults[] findWhereProgramCodeEquals(String programCode) throws ViewedResultsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE Program_Code = ? ORDER BY Program_Code", new Object[] { programCode } );
	}

	/** 
	 * Returns all rows from the viewed_results table that match the criteria 'Competition_Code = :competitionCode'.
	 */
	public ViewedResults[] findWhereCompetitionCodeEquals(String competitionCode) throws ViewedResultsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE Competition_Code = ? ORDER BY Competition_Code", new Object[] { competitionCode } );
	}

	/** 
	 * Returns all rows from the viewed_results table that match the criteria 'Version = :version'.
	 */
	public ViewedResults[] findWhereVersionEquals(String version) throws ViewedResultsDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE Version = ? ORDER BY Version", new Object[] { version } );
	}

	/**
	 * Method 'ViewedResultsDaoImpl'
	 * 
	 */
	public ViewedResultsDaoImpl()
	{
	}

	/**
	 * Method 'ViewedResultsDaoImpl'
	 * 
	 * @param userConn
	 */
	public ViewedResultsDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "eon.viewed_results";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected ViewedResults fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			ViewedResults dto = new ViewedResults();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected ViewedResults[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			ViewedResults dto = new ViewedResults();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		ViewedResults ret[] = new ViewedResults[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(ViewedResults dto, ResultSet rs) throws SQLException
	{
		dto.setNumberFicha( rs.getString( COLUMN_NUMBER_FICHA ) );
		dto.setQuarterName( rs.getString( COLUMN_QUARTER_NAME ) );
		dto.setAcronymsWorkingDay( rs.getString( COLUMN_ACRONYMS_WORKING_DAY ) );
		dto.setSpecialityName( rs.getString( COLUMN_SPECIALITY_NAME ) );
		dto.setLearningResultCode( rs.getString( COLUMN_LEARNING_RESULT_CODE ) );
		dto.setProgramCode( rs.getString( COLUMN_PROGRAM_CODE ) );
		dto.setCompetitionCode( rs.getString( COLUMN_COMPETITION_CODE ) );
		dto.setVersion( rs.getString( COLUMN_VERSION ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(ViewedResults dto)
	{
	}

	/** 
	 * Returns all rows from the viewed_results table that match the specified arbitrary SQL statement
	 */
	public ViewedResults[] findByDynamicSelect(String sql, Object[] sqlParams) throws ViewedResultsDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ViewedResultsDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the viewed_results table that match the specified arbitrary SQL statement
	 */
	public ViewedResults[] findByDynamicWhere(String sql, Object[] sqlParams) throws ViewedResultsDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ViewedResultsDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
