/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package co.edu.sena.jdbc;

import co.edu.sena.dao.*;
import co.edu.sena.factory.*;
import co.edu.sena.dto.*;
import co.edu.sena.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class WorkingDayDaoImpl extends AbstractDAO implements WorkingDayDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT acronyms_Working_Day, name, description, Image, State FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( acronyms_Working_Day, name, description, Image, State ) VALUES ( ?, ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET acronyms_Working_Day = ?, name = ?, description = ?, Image = ?, State = ? WHERE acronyms_Working_Day = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE acronyms_Working_Day = ?";

	/** 
	 * Index of column acronyms_Working_Day
	 */
	protected static final int COLUMN_ACRONYMS_WORKING_DAY = 1;

	/** 
	 * Index of column name
	 */
	protected static final int COLUMN_NAME = 2;

	/** 
	 * Index of column description
	 */
	protected static final int COLUMN_DESCRIPTION = 3;

	/** 
	 * Index of column Image
	 */
	protected static final int COLUMN_IMAGE = 4;

	/** 
	 * Index of column State
	 */
	protected static final int COLUMN_STATE = 5;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 5;

	/** 
	 * Index of primary-key column acronyms_Working_Day
	 */
	protected static final int PK_COLUMN_ACRONYMS_WORKING_DAY = 1;

	/** 
	 * Inserts a new row in the working_day table.
	 */
	public WorkingDayPk insert(WorkingDay dto) throws WorkingDayDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setString( index++, dto.getAcronymsWorkingDay() );
			stmt.setString( index++, dto.getName() );
			stmt.setString( index++, dto.getDescription() );
			super.setBlobColumn(stmt, index++, dto.getImage() );
			if (dto.isStateNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setInt( index++, dto.getState() );
			}
		
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new WorkingDayDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the working_day table.
	 */
	public void update(WorkingDayPk pk, WorkingDay dto) throws WorkingDayDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setString( index++, dto.getAcronymsWorkingDay() );
			stmt.setString( index++, dto.getName() );
			stmt.setString( index++, dto.getDescription() );
			super.setBlobColumn(stmt, index++, dto.getImage() );
			if (dto.isStateNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setInt( index++, dto.getState() );
			}
		
			stmt.setString( 6, pk.getAcronymsWorkingDay() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new WorkingDayDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the working_day table.
	 */
	public void delete(WorkingDayPk pk) throws WorkingDayDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setString( 1, pk.getAcronymsWorkingDay() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new WorkingDayDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the working_day table that matches the specified primary-key value.
	 */
	public WorkingDay findByPrimaryKey(WorkingDayPk pk) throws WorkingDayDaoException
	{
		return findByPrimaryKey( pk.getAcronymsWorkingDay() );
	}

	/** 
	 * Returns all rows from the working_day table that match the criteria 'acronyms_Working_Day = :acronymsWorkingDay'.
	 */
	public WorkingDay findByPrimaryKey(String acronymsWorkingDay) throws WorkingDayDaoException
	{
		WorkingDay ret[] = findByDynamicSelect( SQL_SELECT + " WHERE acronyms_Working_Day = ?", new Object[] { acronymsWorkingDay } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the working_day table that match the criteria ''.
	 */
	public WorkingDay[] findAll() throws WorkingDayDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY acronyms_Working_Day", null );
	}

	/** 
	 * Returns all rows from the working_day table that match the criteria 'acronyms_Working_Day = :acronymsWorkingDay'.
	 */
	public WorkingDay[] findWhereAcronymsWorkingDayEquals(String acronymsWorkingDay) throws WorkingDayDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE acronyms_Working_Day = ? ORDER BY acronyms_Working_Day", new Object[] { acronymsWorkingDay } );
	}

	/** 
	 * Returns all rows from the working_day table that match the criteria 'name = :name'.
	 */
	public WorkingDay[] findWhereNameEquals(String name) throws WorkingDayDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE name = ? ORDER BY name", new Object[] { name } );
	}

	/** 
	 * Returns all rows from the working_day table that match the criteria 'description = :description'.
	 */
	public WorkingDay[] findWhereDescriptionEquals(String description) throws WorkingDayDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE description = ? ORDER BY description", new Object[] { description } );
	}

	/** 
	 * Returns all rows from the working_day table that match the criteria 'Image = :image'.
	 */
	public WorkingDay[] findWhereImageEquals(byte[] image) throws WorkingDayDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE Image = ? ORDER BY Image", new Object[] { image } );
	}

	/** 
	 * Returns all rows from the working_day table that match the criteria 'State = :state'.
	 */
	public WorkingDay[] findWhereStateEquals(int state) throws WorkingDayDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE State = ? ORDER BY State", new Object[] {  new Integer(state) } );
	}

	/**
	 * Method 'WorkingDayDaoImpl'
	 * 
	 */
	public WorkingDayDaoImpl()
	{
	}

	/**
	 * Method 'WorkingDayDaoImpl'
	 * 
	 * @param userConn
	 */
	public WorkingDayDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "eon.working_day";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected WorkingDay fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			WorkingDay dto = new WorkingDay();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected WorkingDay[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			WorkingDay dto = new WorkingDay();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		WorkingDay ret[] = new WorkingDay[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(WorkingDay dto, ResultSet rs) throws SQLException
	{
		dto.setAcronymsWorkingDay( rs.getString( COLUMN_ACRONYMS_WORKING_DAY ) );
		dto.setName( rs.getString( COLUMN_NAME ) );
		dto.setDescription( rs.getString( COLUMN_DESCRIPTION ) );
		dto.setImage( super.getBlobColumn(rs, COLUMN_IMAGE ) );
		dto.setState( rs.getInt( COLUMN_STATE ) );
		if (rs.wasNull()) {
			dto.setStateNull( true );
		}
		
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(WorkingDay dto)
	{
	}

	/** 
	 * Returns all rows from the working_day table that match the specified arbitrary SQL statement
	 */
	public WorkingDay[] findByDynamicSelect(String sql, Object[] sqlParams) throws WorkingDayDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new WorkingDayDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the working_day table that match the specified arbitrary SQL statement
	 */
	public WorkingDay[] findByDynamicWhere(String sql, Object[] sqlParams) throws WorkingDayDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new WorkingDayDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
