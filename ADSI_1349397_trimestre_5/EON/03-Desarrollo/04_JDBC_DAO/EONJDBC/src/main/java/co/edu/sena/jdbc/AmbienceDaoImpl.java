/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package co.edu.sena.jdbc;

import co.edu.sena.dao.*;
import co.edu.sena.factory.*;
import co.edu.sena.dto.*;
import co.edu.sena.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class AmbienceDaoImpl extends AbstractDAO implements AmbienceDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT number_Ambience, Description, Status, name_Headquarters FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( number_Ambience, Description, Status, name_Headquarters ) VALUES ( ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET number_Ambience = ?, Description = ?, Status = ?, name_Headquarters = ? WHERE name_Headquarters = ? AND number_Ambience = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE name_Headquarters = ? AND number_Ambience = ?";

	/** 
	 * Index of column number_Ambience
	 */
	protected static final int COLUMN_NUMBER_AMBIENCE = 1;

	/** 
	 * Index of column Description
	 */
	protected static final int COLUMN_DESCRIPTION = 2;

	/** 
	 * Index of column Status
	 */
	protected static final int COLUMN_STATUS = 3;

	/** 
	 * Index of column name_Headquarters
	 */
	protected static final int COLUMN_NAME_HEADQUARTERS = 4;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 4;

	/** 
	 * Index of primary-key column name_Headquarters
	 */
	protected static final int PK_COLUMN_NAME_HEADQUARTERS = 1;

	/** 
	 * Index of primary-key column number_Ambience
	 */
	protected static final int PK_COLUMN_NUMBER_AMBIENCE = 2;

	/** 
	 * Inserts a new row in the ambience table.
	 */
	public AmbiencePk insert(Ambience dto) throws AmbienceDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setString( index++, dto.getNumberAmbience() );
			stmt.setString( index++, dto.getDescription() );
			if (dto.isStatusNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setShort( index++, dto.getStatus() );
			}
		
			stmt.setString( index++, dto.getNameHeadquarters() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new AmbienceDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the ambience table.
	 */
	public void update(AmbiencePk pk, Ambience dto) throws AmbienceDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setString( index++, dto.getNumberAmbience() );
			stmt.setString( index++, dto.getDescription() );
			if (dto.isStatusNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setShort( index++, dto.getStatus() );
			}
		
			stmt.setString( index++, dto.getNameHeadquarters() );
			stmt.setString( 5, pk.getNameHeadquarters() );
			stmt.setString( 6, pk.getNumberAmbience() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new AmbienceDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the ambience table.
	 */
	public void delete(AmbiencePk pk) throws AmbienceDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setString( 1, pk.getNameHeadquarters() );
			stmt.setString( 2, pk.getNumberAmbience() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new AmbienceDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the ambience table that matches the specified primary-key value.
	 */
	public Ambience findByPrimaryKey(AmbiencePk pk) throws AmbienceDaoException
	{
		return findByPrimaryKey( pk.getNameHeadquarters(), pk.getNumberAmbience() );
	}

	/** 
	 * Returns all rows from the ambience table that match the criteria 'name_Headquarters = :nameHeadquarters AND number_Ambience = :numberAmbience'.
	 */
	public Ambience findByPrimaryKey(String nameHeadquarters, String numberAmbience) throws AmbienceDaoException
	{
		Ambience ret[] = findByDynamicSelect( SQL_SELECT + " WHERE name_Headquarters = ? AND number_Ambience = ?", new Object[] { nameHeadquarters, numberAmbience } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the ambience table that match the criteria ''.
	 */
	public Ambience[] findAll() throws AmbienceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY name_Headquarters, number_Ambience", null );
	}

	/** 
	 * Returns all rows from the ambience table that match the criteria 'name_Headquarters = :nameHeadquarters'.
	 */
	public Ambience[] findByHeadquarters(String nameHeadquarters) throws AmbienceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE name_Headquarters = ?", new Object[] { nameHeadquarters } );
	}

	/** 
	 * Returns all rows from the ambience table that match the criteria 'number_Ambience = :numberAmbience'.
	 */
	public Ambience[] findWhereNumberAmbienceEquals(String numberAmbience) throws AmbienceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE number_Ambience = ? ORDER BY number_Ambience", new Object[] { numberAmbience } );
	}

	/** 
	 * Returns all rows from the ambience table that match the criteria 'Description = :description'.
	 */
	public Ambience[] findWhereDescriptionEquals(String description) throws AmbienceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE Description = ? ORDER BY Description", new Object[] { description } );
	}

	/** 
	 * Returns all rows from the ambience table that match the criteria 'Status = :status'.
	 */
	public Ambience[] findWhereStatusEquals(short status) throws AmbienceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE Status = ? ORDER BY Status", new Object[] {  new Short(status) } );
	}

	/** 
	 * Returns all rows from the ambience table that match the criteria 'name_Headquarters = :nameHeadquarters'.
	 */
	public Ambience[] findWhereNameHeadquartersEquals(String nameHeadquarters) throws AmbienceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE name_Headquarters = ? ORDER BY name_Headquarters", new Object[] { nameHeadquarters } );
	}

	/**
	 * Method 'AmbienceDaoImpl'
	 * 
	 */
	public AmbienceDaoImpl()
	{
	}

	/**
	 * Method 'AmbienceDaoImpl'
	 * 
	 * @param userConn
	 */
	public AmbienceDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "eon.ambience";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected Ambience fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			Ambience dto = new Ambience();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected Ambience[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			Ambience dto = new Ambience();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		Ambience ret[] = new Ambience[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(Ambience dto, ResultSet rs) throws SQLException
	{
		dto.setNumberAmbience( rs.getString( COLUMN_NUMBER_AMBIENCE ) );
		dto.setDescription( rs.getString( COLUMN_DESCRIPTION ) );
		dto.setStatus( rs.getShort( COLUMN_STATUS ) );
		if (rs.wasNull()) {
			dto.setStatusNull( true );
		}
		
		dto.setNameHeadquarters( rs.getString( COLUMN_NAME_HEADQUARTERS ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(Ambience dto)
	{
	}

	/** 
	 * Returns all rows from the ambience table that match the specified arbitrary SQL statement
	 */
	public Ambience[] findByDynamicSelect(String sql, Object[] sqlParams) throws AmbienceDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new AmbienceDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the ambience table that match the specified arbitrary SQL statement
	 */
	public Ambience[] findByDynamicWhere(String sql, Object[] sqlParams) throws AmbienceDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new AmbienceDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
