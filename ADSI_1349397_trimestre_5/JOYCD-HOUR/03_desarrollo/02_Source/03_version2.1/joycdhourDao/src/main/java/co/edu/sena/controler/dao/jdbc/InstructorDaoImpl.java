/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package co.edu.sena.controler.dao.jdbc;

import co.edu.sena.controler.dao.dao.*;
import co.edu.sena.controler.dao.factory.*;
import co.edu.sena.controler.dao.dto.*;
import co.edu.sena.controler.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class InstructorDaoImpl extends AbstractDAO implements InstructorDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT document_type, identification_card, type_bonding, name_speciality FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( document_type, identification_card, type_bonding, name_speciality ) VALUES ( ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET document_type = ?, identification_card = ?, type_bonding = ?, name_speciality = ? WHERE document_type = ? AND identification_card = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE document_type = ? AND identification_card = ?";

	/** 
	 * Index of column document_type
	 */
	protected static final int COLUMN_DOCUMENT_TYPE = 1;

	/** 
	 * Index of column identification_card
	 */
	protected static final int COLUMN_IDENTIFICATION_CARD = 2;

	/** 
	 * Index of column type_bonding
	 */
	protected static final int COLUMN_TYPE_BONDING = 3;

	/** 
	 * Index of column name_speciality
	 */
	protected static final int COLUMN_NAME_SPECIALITY = 4;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 4;

	/** 
	 * Index of primary-key column document_type
	 */
	protected static final int PK_COLUMN_DOCUMENT_TYPE = 1;

	/** 
	 * Index of primary-key column identification_card
	 */
	protected static final int PK_COLUMN_IDENTIFICATION_CARD = 2;

	/** 
	 * Inserts a new row in the instructor table.
	 */
	public InstructorPk insert(Instructor dto) throws InstructorDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setString( index++, dto.getDocumentType() );
			stmt.setString( index++, dto.getIdentificationCard() );
			stmt.setString( index++, dto.getTypeBonding() );
			stmt.setString( index++, dto.getNameSpeciality() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new InstructorDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the instructor table.
	 */
	public void update(InstructorPk pk, Instructor dto) throws InstructorDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setString( index++, dto.getDocumentType() );
			stmt.setString( index++, dto.getIdentificationCard() );
			stmt.setString( index++, dto.getTypeBonding() );
			stmt.setString( index++, dto.getNameSpeciality() );
			stmt.setString( 5, pk.getDocumentType() );
			stmt.setString( 6, pk.getIdentificationCard() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new InstructorDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the instructor table.
	 */
	public void delete(InstructorPk pk) throws InstructorDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setString( 1, pk.getDocumentType() );
			stmt.setString( 2, pk.getIdentificationCard() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new InstructorDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the instructor table that matches the specified primary-key value.
	 */
	public Instructor findByPrimaryKey(InstructorPk pk) throws InstructorDaoException
	{
		return findByPrimaryKey( pk.getDocumentType(), pk.getIdentificationCard() );
	}

	/** 
	 * Returns all rows from the instructor table that match the criteria 'document_type = :documentType AND identification_card = :identificationCard'.
	 */
	public Instructor findByPrimaryKey(String documentType, String identificationCard) throws InstructorDaoException
	{
		Instructor ret[] = findByDynamicSelect( SQL_SELECT + " WHERE document_type = ? AND identification_card = ?", new Object[] { documentType, identificationCard } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the instructor table that match the criteria ''.
	 */
	public Instructor[] findAll() throws InstructorDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY document_type, identification_card", null );
	}

	/** 
	 * Returns all rows from the instructor table that match the criteria 'type_bonding = :typeBonding'.
	 */
	public Instructor[] findByBonding(String typeBonding) throws InstructorDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE type_bonding = ?", new Object[] { typeBonding } );
	}

	/** 
	 * Returns all rows from the instructor table that match the criteria 'document_type = :documentType AND identification_card = :identificationCard'.
	 */
	public Instructor[] findByCustomer(String documentType, String identificationCard) throws InstructorDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE document_type = ? AND identification_card = ?", new Object[] { documentType, identificationCard } );
	}

	/** 
	 * Returns all rows from the instructor table that match the criteria 'name_speciality = :nameSpeciality'.
	 */
	public Instructor[] findBySpeciality(String nameSpeciality) throws InstructorDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE name_speciality = ?", new Object[] { nameSpeciality } );
	}

	/** 
	 * Returns all rows from the instructor table that match the criteria 'document_type = :documentType'.
	 */
	public Instructor[] findWhereDocumentTypeEquals(String documentType) throws InstructorDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE document_type = ? ORDER BY document_type", new Object[] { documentType } );
	}

	/** 
	 * Returns all rows from the instructor table that match the criteria 'identification_card = :identificationCard'.
	 */
	public Instructor[] findWhereIdentificationCardEquals(String identificationCard) throws InstructorDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE identification_card = ? ORDER BY identification_card", new Object[] { identificationCard } );
	}

	/** 
	 * Returns all rows from the instructor table that match the criteria 'type_bonding = :typeBonding'.
	 */
	public Instructor[] findWhereTypeBondingEquals(String typeBonding) throws InstructorDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE type_bonding = ? ORDER BY type_bonding", new Object[] { typeBonding } );
	}

	/** 
	 * Returns all rows from the instructor table that match the criteria 'name_speciality = :nameSpeciality'.
	 */
	public Instructor[] findWhereNameSpecialityEquals(String nameSpeciality) throws InstructorDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE name_speciality = ? ORDER BY name_speciality", new Object[] { nameSpeciality } );
	}

	/**
	 * Method 'InstructorDaoImpl'
	 * 
	 */
	public InstructorDaoImpl()
	{
	}

	/**
	 * Method 'InstructorDaoImpl'
	 * 
	 * @param userConn
	 */
	public InstructorDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "joycd.instructor";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected Instructor fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			Instructor dto = new Instructor();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected Instructor[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			Instructor dto = new Instructor();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		Instructor ret[] = new Instructor[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(Instructor dto, ResultSet rs) throws SQLException
	{
		dto.setDocumentType( rs.getString( COLUMN_DOCUMENT_TYPE ) );
		dto.setIdentificationCard( rs.getString( COLUMN_IDENTIFICATION_CARD ) );
		dto.setTypeBonding( rs.getString( COLUMN_TYPE_BONDING ) );
		dto.setNameSpeciality( rs.getString( COLUMN_NAME_SPECIALITY ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(Instructor dto)
	{
	}

	/** 
	 * Returns all rows from the instructor table that match the specified arbitrary SQL statement
	 */
	public Instructor[] findByDynamicSelect(String sql, Object[] sqlParams) throws InstructorDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new InstructorDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the instructor table that match the specified arbitrary SQL statement
	 */
	public Instructor[] findByDynamicWhere(String sql, Object[] sqlParams) throws InstructorDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new InstructorDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
