/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package co.edu.sena.controler.dao.jdbc;

import co.edu.sena.controler.dao.dao.*;
import co.edu.sena.controler.dao.factory.*;
import java.util.Date;
import co.edu.sena.controler.dao.dto.*;
import co.edu.sena.controler.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class ActualityTrimesterDaoImpl extends AbstractDAO implements ActualityTrimesterDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT actuality_trimester, date_start, date_finish FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( actuality_trimester, date_start, date_finish ) VALUES ( ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET actuality_trimester = ?, date_start = ?, date_finish = ? WHERE actuality_trimester = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE actuality_trimester = ?";

	/** 
	 * Index of column actuality_trimester
	 */
	protected static final int COLUMN_ACTUALITY_TRIMESTER = 1;

	/** 
	 * Index of column date_start
	 */
	protected static final int COLUMN_DATE_START = 2;

	/** 
	 * Index of column date_finish
	 */
	protected static final int COLUMN_DATE_FINISH = 3;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 3;

	/** 
	 * Index of primary-key column actuality_trimester
	 */
	protected static final int PK_COLUMN_ACTUALITY_TRIMESTER = 1;

	/** 
	 * Inserts a new row in the actuality_trimester table.
	 */
	public ActualityTrimesterPk insert(ActualityTrimester dto) throws ActualityTrimesterDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setString( index++, dto.getActualityTrimester() );
			stmt.setDate(index++, dto.getDateStart()==null ? null : new java.sql.Date( dto.getDateStart().getTime() ) );
			stmt.setDate(index++, dto.getDateFinish()==null ? null : new java.sql.Date( dto.getDateFinish().getTime() ) );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ActualityTrimesterDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the actuality_trimester table.
	 */
	public void update(ActualityTrimesterPk pk, ActualityTrimester dto) throws ActualityTrimesterDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setString( index++, dto.getActualityTrimester() );
			stmt.setDate(index++, dto.getDateStart()==null ? null : new java.sql.Date( dto.getDateStart().getTime() ) );
			stmt.setDate(index++, dto.getDateFinish()==null ? null : new java.sql.Date( dto.getDateFinish().getTime() ) );
			stmt.setString( 4, pk.getActualityTrimester() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ActualityTrimesterDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the actuality_trimester table.
	 */
	public void delete(ActualityTrimesterPk pk) throws ActualityTrimesterDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setString( 1, pk.getActualityTrimester() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ActualityTrimesterDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the actuality_trimester table that matches the specified primary-key value.
	 */
	public ActualityTrimester findByPrimaryKey(ActualityTrimesterPk pk) throws ActualityTrimesterDaoException
	{
		return findByPrimaryKey( pk.getActualityTrimester() );
	}

	/** 
	 * Returns all rows from the actuality_trimester table that match the criteria 'actuality_trimester = :actualityTrimester'.
	 */
	public ActualityTrimester findByPrimaryKey(String actualityTrimester) throws ActualityTrimesterDaoException
	{
		ActualityTrimester ret[] = findByDynamicSelect( SQL_SELECT + " WHERE actuality_trimester = ?", new Object[] { actualityTrimester } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the actuality_trimester table that match the criteria ''.
	 */
	public ActualityTrimester[] findAll() throws ActualityTrimesterDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY actuality_trimester", null );
	}

	/** 
	 * Returns all rows from the actuality_trimester table that match the criteria 'actuality_trimester = :actualityTrimester'.
	 */
	public ActualityTrimester[] findWhereActualityTrimesterEquals(String actualityTrimester) throws ActualityTrimesterDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE actuality_trimester = ? ORDER BY actuality_trimester", new Object[] { actualityTrimester } );
	}

	/** 
	 * Returns all rows from the actuality_trimester table that match the criteria 'date_start = :dateStart'.
	 */
	public ActualityTrimester[] findWhereDateStartEquals(Date dateStart) throws ActualityTrimesterDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE date_start = ? ORDER BY date_start", new Object[] { dateStart==null ? null : new java.sql.Date( dateStart.getTime() ) } );
	}

	/** 
	 * Returns all rows from the actuality_trimester table that match the criteria 'date_finish = :dateFinish'.
	 */
	public ActualityTrimester[] findWhereDateFinishEquals(Date dateFinish) throws ActualityTrimesterDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE date_finish = ? ORDER BY date_finish", new Object[] { dateFinish==null ? null : new java.sql.Date( dateFinish.getTime() ) } );
	}

	/**
	 * Method 'ActualityTrimesterDaoImpl'
	 * 
	 */
	public ActualityTrimesterDaoImpl()
	{
	}

	/**
	 * Method 'ActualityTrimesterDaoImpl'
	 * 
	 * @param userConn
	 */
	public ActualityTrimesterDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "joycd.actuality_trimester";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected ActualityTrimester fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			ActualityTrimester dto = new ActualityTrimester();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected ActualityTrimester[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			ActualityTrimester dto = new ActualityTrimester();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		ActualityTrimester ret[] = new ActualityTrimester[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(ActualityTrimester dto, ResultSet rs) throws SQLException
	{
		dto.setActualityTrimester( rs.getString( COLUMN_ACTUALITY_TRIMESTER ) );
		dto.setDateStart( rs.getDate(COLUMN_DATE_START ) );
		dto.setDateFinish( rs.getDate(COLUMN_DATE_FINISH ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(ActualityTrimester dto)
	{
	}

	/** 
	 * Returns all rows from the actuality_trimester table that match the specified arbitrary SQL statement
	 */
	public ActualityTrimester[] findByDynamicSelect(String sql, Object[] sqlParams) throws ActualityTrimesterDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ActualityTrimesterDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the actuality_trimester table that match the specified arbitrary SQL statement
	 */
	public ActualityTrimester[] findByDynamicWhere(String sql, Object[] sqlParams) throws ActualityTrimesterDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ActualityTrimesterDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
