/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package co.edu.sena.controler.dao.jdbc;

import co.edu.sena.controler.dao.dao.*;
import co.edu.sena.controler.dao.factory.*;
import co.edu.sena.controler.dao.dto.*;
import co.edu.sena.controler.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class EmailServerDaoImpl extends AbstractDAO implements EmailServerDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT email, password, smtp_host, smtp_port, smtp_start_tls_enable, smtp_auth, recovery_order, recovery_message FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( email, password, smtp_host, smtp_port, smtp_start_tls_enable, smtp_auth, recovery_order, recovery_message ) VALUES ( ?, ?, ?, ?, ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET email = ?, password = ?, smtp_host = ?, smtp_port = ?, smtp_start_tls_enable = ?, smtp_auth = ?, recovery_order = ?, recovery_message = ? WHERE email = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE email = ?";

	/** 
	 * Index of column email
	 */
	protected static final int COLUMN_EMAIL = 1;

	/** 
	 * Index of column password
	 */
	protected static final int COLUMN_PASSWORD = 2;

	/** 
	 * Index of column smtp_host
	 */
	protected static final int COLUMN_SMTP_HOST = 3;

	/** 
	 * Index of column smtp_port
	 */
	protected static final int COLUMN_SMTP_PORT = 4;

	/** 
	 * Index of column smtp_start_tls_enable
	 */
	protected static final int COLUMN_SMTP_START_TLS_ENABLE = 5;

	/** 
	 * Index of column smtp_auth
	 */
	protected static final int COLUMN_SMTP_AUTH = 6;

	/** 
	 * Index of column recovery_order
	 */
	protected static final int COLUMN_RECOVERY_ORDER = 7;

	/** 
	 * Index of column recovery_message
	 */
	protected static final int COLUMN_RECOVERY_MESSAGE = 8;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 8;

	/** 
	 * Index of primary-key column email
	 */
	protected static final int PK_COLUMN_EMAIL = 1;

	/** 
	 * Inserts a new row in the email_server table.
	 */
	public EmailServerPk insert(EmailServer dto) throws EmailServerDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setString( index++, dto.getEmail() );
			stmt.setString( index++, dto.getPassword() );
			stmt.setString( index++, dto.getSmtpHost() );
			if (dto.isSmtpPortNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setInt( index++, dto.getSmtpPort() );
			}
		
			if (dto.isSmtpStartTlsEnableNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setShort( index++, dto.getSmtpStartTlsEnable() );
			}
		
			if (dto.isSmtpAuthNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setShort( index++, dto.getSmtpAuth() );
			}
		
			stmt.setString( index++, dto.getRecoveryOrder() );
			stmt.setString( index++, dto.getRecoveryMessage() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmailServerDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the email_server table.
	 */
	public void update(EmailServerPk pk, EmailServer dto) throws EmailServerDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setString( index++, dto.getEmail() );
			stmt.setString( index++, dto.getPassword() );
			stmt.setString( index++, dto.getSmtpHost() );
			if (dto.isSmtpPortNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setInt( index++, dto.getSmtpPort() );
			}
		
			if (dto.isSmtpStartTlsEnableNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setShort( index++, dto.getSmtpStartTlsEnable() );
			}
		
			if (dto.isSmtpAuthNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setShort( index++, dto.getSmtpAuth() );
			}
		
			stmt.setString( index++, dto.getRecoveryOrder() );
			stmt.setString( index++, dto.getRecoveryMessage() );
			stmt.setString( 9, pk.getEmail() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmailServerDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the email_server table.
	 */
	public void delete(EmailServerPk pk) throws EmailServerDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setString( 1, pk.getEmail() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmailServerDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the email_server table that matches the specified primary-key value.
	 */
	public EmailServer findByPrimaryKey(EmailServerPk pk) throws EmailServerDaoException
	{
		return findByPrimaryKey( pk.getEmail() );
	}

	/** 
	 * Returns all rows from the email_server table that match the criteria 'email = :email'.
	 */
	public EmailServer findByPrimaryKey(String email) throws EmailServerDaoException
	{
		EmailServer ret[] = findByDynamicSelect( SQL_SELECT + " WHERE email = ?", new Object[] { email } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the email_server table that match the criteria ''.
	 */
	public EmailServer[] findAll() throws EmailServerDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY email", null );
	}

	/** 
	 * Returns all rows from the email_server table that match the criteria 'email = :email'.
	 */
	public EmailServer[] findWhereEmailEquals(String email) throws EmailServerDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE email = ? ORDER BY email", new Object[] { email } );
	}

	/** 
	 * Returns all rows from the email_server table that match the criteria 'password = :password'.
	 */
	public EmailServer[] findWherePasswordEquals(String password) throws EmailServerDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE password = ? ORDER BY password", new Object[] { password } );
	}

	/** 
	 * Returns all rows from the email_server table that match the criteria 'smtp_host = :smtpHost'.
	 */
	public EmailServer[] findWhereSmtpHostEquals(String smtpHost) throws EmailServerDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE smtp_host = ? ORDER BY smtp_host", new Object[] { smtpHost } );
	}

	/** 
	 * Returns all rows from the email_server table that match the criteria 'smtp_port = :smtpPort'.
	 */
	public EmailServer[] findWhereSmtpPortEquals(int smtpPort) throws EmailServerDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE smtp_port = ? ORDER BY smtp_port", new Object[] {  new Integer(smtpPort) } );
	}

	/** 
	 * Returns all rows from the email_server table that match the criteria 'smtp_start_tls_enable = :smtpStartTlsEnable'.
	 */
	public EmailServer[] findWhereSmtpStartTlsEnableEquals(short smtpStartTlsEnable) throws EmailServerDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE smtp_start_tls_enable = ? ORDER BY smtp_start_tls_enable", new Object[] {  new Short(smtpStartTlsEnable) } );
	}

	/** 
	 * Returns all rows from the email_server table that match the criteria 'smtp_auth = :smtpAuth'.
	 */
	public EmailServer[] findWhereSmtpAuthEquals(short smtpAuth) throws EmailServerDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE smtp_auth = ? ORDER BY smtp_auth", new Object[] {  new Short(smtpAuth) } );
	}

	/** 
	 * Returns all rows from the email_server table that match the criteria 'recovery_order = :recoveryOrder'.
	 */
	public EmailServer[] findWhereRecoveryOrderEquals(String recoveryOrder) throws EmailServerDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE recovery_order = ? ORDER BY recovery_order", new Object[] { recoveryOrder } );
	}

	/** 
	 * Returns all rows from the email_server table that match the criteria 'recovery_message = :recoveryMessage'.
	 */
	public EmailServer[] findWhereRecoveryMessageEquals(String recoveryMessage) throws EmailServerDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE recovery_message = ? ORDER BY recovery_message", new Object[] { recoveryMessage } );
	}

	/**
	 * Method 'EmailServerDaoImpl'
	 * 
	 */
	public EmailServerDaoImpl()
	{
	}

	/**
	 * Method 'EmailServerDaoImpl'
	 * 
	 * @param userConn
	 */
	public EmailServerDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "joycd.email_server";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected EmailServer fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			EmailServer dto = new EmailServer();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected EmailServer[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			EmailServer dto = new EmailServer();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		EmailServer ret[] = new EmailServer[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(EmailServer dto, ResultSet rs) throws SQLException
	{
		dto.setEmail( rs.getString( COLUMN_EMAIL ) );
		dto.setPassword( rs.getString( COLUMN_PASSWORD ) );
		dto.setSmtpHost( rs.getString( COLUMN_SMTP_HOST ) );
		dto.setSmtpPort( rs.getInt( COLUMN_SMTP_PORT ) );
		if (rs.wasNull()) {
			dto.setSmtpPortNull( true );
		}
		
		dto.setSmtpStartTlsEnable( rs.getShort( COLUMN_SMTP_START_TLS_ENABLE ) );
		if (rs.wasNull()) {
			dto.setSmtpStartTlsEnableNull( true );
		}
		
		dto.setSmtpAuth( rs.getShort( COLUMN_SMTP_AUTH ) );
		if (rs.wasNull()) {
			dto.setSmtpAuthNull( true );
		}
		
		dto.setRecoveryOrder( rs.getString( COLUMN_RECOVERY_ORDER ) );
		dto.setRecoveryMessage( rs.getString( COLUMN_RECOVERY_MESSAGE ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(EmailServer dto)
	{
	}

	/** 
	 * Returns all rows from the email_server table that match the specified arbitrary SQL statement
	 */
	public EmailServer[] findByDynamicSelect(String sql, Object[] sqlParams) throws EmailServerDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmailServerDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the email_server table that match the specified arbitrary SQL statement
	 */
	public EmailServer[] findByDynamicWhere(String sql, Object[] sqlParams) throws EmailServerDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EmailServerDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
