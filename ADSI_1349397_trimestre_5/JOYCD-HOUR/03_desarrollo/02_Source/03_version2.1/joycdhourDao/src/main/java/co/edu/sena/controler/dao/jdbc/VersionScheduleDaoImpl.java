/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package co.edu.sena.controler.dao.jdbc;

import co.edu.sena.controler.dao.dao.*;
import co.edu.sena.controler.dao.factory.*;
import co.edu.sena.controler.dao.dto.*;
import co.edu.sena.controler.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class VersionScheduleDaoImpl extends AbstractDAO implements VersionScheduleDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT version, status_version, actuality_trimester FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( version, status_version, actuality_trimester ) VALUES ( ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET version = ?, status_version = ?, actuality_trimester = ? WHERE version = ? AND actuality_trimester = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE version = ? AND actuality_trimester = ?";

	/** 
	 * Index of column version
	 */
	protected static final int COLUMN_VERSION = 1;

	/** 
	 * Index of column status_version
	 */
	protected static final int COLUMN_STATUS_VERSION = 2;

	/** 
	 * Index of column actuality_trimester
	 */
	protected static final int COLUMN_ACTUALITY_TRIMESTER = 3;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 3;

	/** 
	 * Index of primary-key column version
	 */
	protected static final int PK_COLUMN_VERSION = 1;

	/** 
	 * Index of primary-key column actuality_trimester
	 */
	protected static final int PK_COLUMN_ACTUALITY_TRIMESTER = 2;

	/** 
	 * Inserts a new row in the version_schedule table.
	 */
	public VersionSchedulePk insert(VersionSchedule dto) throws VersionScheduleDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setInt( index++, dto.getVersion() );
			stmt.setShort( index++, dto.getStatusVersion() );
			stmt.setString( index++, dto.getActualityTrimester() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new VersionScheduleDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the version_schedule table.
	 */
	public void update(VersionSchedulePk pk, VersionSchedule dto) throws VersionScheduleDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setInt( index++, dto.getVersion() );
			stmt.setShort( index++, dto.getStatusVersion() );
			stmt.setString( index++, dto.getActualityTrimester() );
			stmt.setInt( 4, pk.getVersion() );
			stmt.setString( 5, pk.getActualityTrimester() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new VersionScheduleDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the version_schedule table.
	 */
	public void delete(VersionSchedulePk pk) throws VersionScheduleDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getVersion() );
			stmt.setString( 2, pk.getActualityTrimester() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new VersionScheduleDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the version_schedule table that matches the specified primary-key value.
	 */
	public VersionSchedule findByPrimaryKey(VersionSchedulePk pk) throws VersionScheduleDaoException
	{
		return findByPrimaryKey( pk.getVersion(), pk.getActualityTrimester() );
	}

	/** 
	 * Returns all rows from the version_schedule table that match the criteria 'version = :version AND actuality_trimester = :actualityTrimester'.
	 */
	public VersionSchedule findByPrimaryKey(int version, String actualityTrimester) throws VersionScheduleDaoException
	{
		VersionSchedule ret[] = findByDynamicSelect( SQL_SELECT + " WHERE version = ? AND actuality_trimester = ?", new Object[] {  new Integer(version), actualityTrimester } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the version_schedule table that match the criteria ''.
	 */
	public VersionSchedule[] findAll() throws VersionScheduleDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY version, actuality_trimester", null );
	}

	/** 
	 * Returns all rows from the version_schedule table that match the criteria 'actuality_trimester = :actualityTrimester'.
	 */
	public VersionSchedule[] findByActualityTrimester(String actualityTrimester) throws VersionScheduleDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE actuality_trimester = ?", new Object[] { actualityTrimester } );
	}

	/** 
	 * Returns all rows from the version_schedule table that match the criteria 'version = :version'.
	 */
	public VersionSchedule[] findWhereVersionEquals(int version) throws VersionScheduleDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE version = ? ORDER BY version", new Object[] {  new Integer(version) } );
	}

	/** 
	 * Returns all rows from the version_schedule table that match the criteria 'status_version = :statusVersion'.
	 */
	public VersionSchedule[] findWhereStatusVersionEquals(short statusVersion) throws VersionScheduleDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE status_version = ? ORDER BY status_version", new Object[] {  new Short(statusVersion) } );
	}

	/** 
	 * Returns all rows from the version_schedule table that match the criteria 'actuality_trimester = :actualityTrimester'.
	 */
	public VersionSchedule[] findWhereActualityTrimesterEquals(String actualityTrimester) throws VersionScheduleDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE actuality_trimester = ? ORDER BY actuality_trimester", new Object[] { actualityTrimester } );
	}

	/**
	 * Method 'VersionScheduleDaoImpl'
	 * 
	 */
	public VersionScheduleDaoImpl()
	{
	}

	/**
	 * Method 'VersionScheduleDaoImpl'
	 * 
	 * @param userConn
	 */
	public VersionScheduleDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "joycd.version_schedule";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected VersionSchedule fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			VersionSchedule dto = new VersionSchedule();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected VersionSchedule[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			VersionSchedule dto = new VersionSchedule();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		VersionSchedule ret[] = new VersionSchedule[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(VersionSchedule dto, ResultSet rs) throws SQLException
	{
		dto.setVersion( rs.getInt( COLUMN_VERSION ) );
		dto.setStatusVersion( rs.getShort( COLUMN_STATUS_VERSION ) );
		dto.setActualityTrimester( rs.getString( COLUMN_ACTUALITY_TRIMESTER ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(VersionSchedule dto)
	{
	}

	/** 
	 * Returns all rows from the version_schedule table that match the specified arbitrary SQL statement
	 */
	public VersionSchedule[] findByDynamicSelect(String sql, Object[] sqlParams) throws VersionScheduleDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new VersionScheduleDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the version_schedule table that match the specified arbitrary SQL statement
	 */
	public VersionSchedule[] findByDynamicWhere(String sql, Object[] sqlParams) throws VersionScheduleDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new VersionScheduleDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
