/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package co.edu.sena.controler.dao.jdbc;

import co.edu.sena.controler.dao.dao.*;
import co.edu.sena.controler.dao.factory.*;
import co.edu.sena.controler.dao.dto.*;
import co.edu.sena.controler.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class ProjectDaoImpl extends AbstractDAO implements ProjectDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT code_project, name_project, status_project FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( code_project, name_project, status_project ) VALUES ( ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET code_project = ?, name_project = ?, status_project = ? WHERE code_project = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE code_project = ?";

	/** 
	 * Index of column code_project
	 */
	protected static final int COLUMN_CODE_PROJECT = 1;

	/** 
	 * Index of column name_project
	 */
	protected static final int COLUMN_NAME_PROJECT = 2;

	/** 
	 * Index of column status_project
	 */
	protected static final int COLUMN_STATUS_PROJECT = 3;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 3;

	/** 
	 * Index of primary-key column code_project
	 */
	protected static final int PK_COLUMN_CODE_PROJECT = 1;

	/** 
	 * Inserts a new row in the project table.
	 */
	public ProjectPk insert(Project dto) throws ProjectDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setString( index++, dto.getCodeProject() );
			stmt.setString( index++, dto.getNameProject() );
			stmt.setShort( index++, dto.getStatusProject() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ProjectDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the project table.
	 */
	public void update(ProjectPk pk, Project dto) throws ProjectDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setString( index++, dto.getCodeProject() );
			stmt.setString( index++, dto.getNameProject() );
			stmt.setShort( index++, dto.getStatusProject() );
			stmt.setString( 4, pk.getCodeProject() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ProjectDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the project table.
	 */
	public void delete(ProjectPk pk) throws ProjectDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setString( 1, pk.getCodeProject() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ProjectDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the project table that matches the specified primary-key value.
	 */
	public Project findByPrimaryKey(ProjectPk pk) throws ProjectDaoException
	{
		return findByPrimaryKey( pk.getCodeProject() );
	}

	/** 
	 * Returns all rows from the project table that match the criteria 'code_project = :codeProject'.
	 */
	public Project findByPrimaryKey(String codeProject) throws ProjectDaoException
	{
		Project ret[] = findByDynamicSelect( SQL_SELECT + " WHERE code_project = ?", new Object[] { codeProject } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the project table that match the criteria ''.
	 */
	public Project[] findAll() throws ProjectDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY code_project", null );
	}

	/** 
	 * Returns all rows from the project table that match the criteria 'code_project = :codeProject'.
	 */
	public Project[] findWhereCodeProjectEquals(String codeProject) throws ProjectDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE code_project = ? ORDER BY code_project", new Object[] { codeProject } );
	}

	/** 
	 * Returns all rows from the project table that match the criteria 'name_project = :nameProject'.
	 */
	public Project[] findWhereNameProjectEquals(String nameProject) throws ProjectDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE name_project = ? ORDER BY name_project", new Object[] { nameProject } );
	}

	/** 
	 * Returns all rows from the project table that match the criteria 'status_project = :statusProject'.
	 */
	public Project[] findWhereStatusProjectEquals(short statusProject) throws ProjectDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE status_project = ? ORDER BY status_project", new Object[] {  new Short(statusProject) } );
	}

	/**
	 * Method 'ProjectDaoImpl'
	 * 
	 */
	public ProjectDaoImpl()
	{
	}

	/**
	 * Method 'ProjectDaoImpl'
	 * 
	 * @param userConn
	 */
	public ProjectDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "joycd.project";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected Project fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			Project dto = new Project();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected Project[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			Project dto = new Project();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		Project ret[] = new Project[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(Project dto, ResultSet rs) throws SQLException
	{
		dto.setCodeProject( rs.getString( COLUMN_CODE_PROJECT ) );
		dto.setNameProject( rs.getString( COLUMN_NAME_PROJECT ) );
		dto.setStatusProject( rs.getShort( COLUMN_STATUS_PROJECT ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(Project dto)
	{
	}

	/** 
	 * Returns all rows from the project table that match the specified arbitrary SQL statement
	 */
	public Project[] findByDynamicSelect(String sql, Object[] sqlParams) throws ProjectDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ProjectDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the project table that match the specified arbitrary SQL statement
	 */
	public Project[] findByDynamicWhere(String sql, Object[] sqlParams) throws ProjectDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ProjectDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
