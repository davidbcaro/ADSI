/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package co.edu.sena.controler.dao.jdbc;

import co.edu.sena.controler.dao.dao.*;
import co.edu.sena.controler.dao.factory.*;
import co.edu.sena.controler.dao.dto.*;
import co.edu.sena.controler.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class CompetenceDaoImpl extends AbstractDAO implements CompetenceDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT competence_code, description, program_code, version FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( competence_code, description, program_code, version ) VALUES ( ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET competence_code = ?, description = ?, program_code = ?, version = ? WHERE competence_code = ? AND program_code = ? AND version = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE competence_code = ? AND program_code = ? AND version = ?";

	/** 
	 * Index of column competence_code
	 */
	protected static final int COLUMN_COMPETENCE_CODE = 1;

	/** 
	 * Index of column description
	 */
	protected static final int COLUMN_DESCRIPTION = 2;

	/** 
	 * Index of column program_code
	 */
	protected static final int COLUMN_PROGRAM_CODE = 3;

	/** 
	 * Index of column version
	 */
	protected static final int COLUMN_VERSION = 4;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 4;

	/** 
	 * Index of primary-key column competence_code
	 */
	protected static final int PK_COLUMN_COMPETENCE_CODE = 1;

	/** 
	 * Index of primary-key column program_code
	 */
	protected static final int PK_COLUMN_PROGRAM_CODE = 2;

	/** 
	 * Index of primary-key column version
	 */
	protected static final int PK_COLUMN_VERSION = 3;

	/** 
	 * Inserts a new row in the competence table.
	 */
	public CompetencePk insert(Competence dto) throws CompetenceDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setString( index++, dto.getCompetenceCode() );
			stmt.setString( index++, dto.getDescription() );
			stmt.setString( index++, dto.getProgramCode() );
			stmt.setString( index++, dto.getVersion() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new CompetenceDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the competence table.
	 */
	public void update(CompetencePk pk, Competence dto) throws CompetenceDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setString( index++, dto.getCompetenceCode() );
			stmt.setString( index++, dto.getDescription() );
			stmt.setString( index++, dto.getProgramCode() );
			stmt.setString( index++, dto.getVersion() );
			stmt.setString( 5, pk.getCompetenceCode() );
			stmt.setString( 6, pk.getProgramCode() );
			stmt.setString( 7, pk.getVersion() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new CompetenceDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the competence table.
	 */
	public void delete(CompetencePk pk) throws CompetenceDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setString( 1, pk.getCompetenceCode() );
			stmt.setString( 2, pk.getProgramCode() );
			stmt.setString( 3, pk.getVersion() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new CompetenceDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the competence table that matches the specified primary-key value.
	 */
	public Competence findByPrimaryKey(CompetencePk pk) throws CompetenceDaoException
	{
		return findByPrimaryKey( pk.getCompetenceCode(), pk.getProgramCode(), pk.getVersion() );
	}

	/** 
	 * Returns all rows from the competence table that match the criteria 'competence_code = :competenceCode AND program_code = :programCode AND version = :version'.
	 */
	public Competence findByPrimaryKey(String competenceCode, String programCode, String version) throws CompetenceDaoException
	{
		Competence ret[] = findByDynamicSelect( SQL_SELECT + " WHERE competence_code = ? AND program_code = ? AND version = ?", new Object[] { competenceCode, programCode, version } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the competence table that match the criteria ''.
	 */
	public Competence[] findAll() throws CompetenceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY competence_code, program_code, version", null );
	}

	/** 
	 * Returns all rows from the competence table that match the criteria 'program_code = :programCode AND version = :version'.
	 */
	public Competence[] findByProgram(String programCode, String version) throws CompetenceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE program_code = ? AND version = ?", new Object[] { programCode, version } );
	}

	/** 
	 * Returns all rows from the competence table that match the criteria 'competence_code = :competenceCode'.
	 */
	public Competence[] findWhereCompetenceCodeEquals(String competenceCode) throws CompetenceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE competence_code = ? ORDER BY competence_code", new Object[] { competenceCode } );
	}

	/** 
	 * Returns all rows from the competence table that match the criteria 'description = :description'.
	 */
	public Competence[] findWhereDescriptionEquals(String description) throws CompetenceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE description = ? ORDER BY description", new Object[] { description } );
	}

	/** 
	 * Returns all rows from the competence table that match the criteria 'program_code = :programCode'.
	 */
	public Competence[] findWhereProgramCodeEquals(String programCode) throws CompetenceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE program_code = ? ORDER BY program_code", new Object[] { programCode } );
	}

	/** 
	 * Returns all rows from the competence table that match the criteria 'version = :version'.
	 */
	public Competence[] findWhereVersionEquals(String version) throws CompetenceDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE version = ? ORDER BY version", new Object[] { version } );
	}

	/**
	 * Method 'CompetenceDaoImpl'
	 * 
	 */
	public CompetenceDaoImpl()
	{
	}

	/**
	 * Method 'CompetenceDaoImpl'
	 * 
	 * @param userConn
	 */
	public CompetenceDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "joycd.competence";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected Competence fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			Competence dto = new Competence();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected Competence[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			Competence dto = new Competence();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		Competence ret[] = new Competence[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(Competence dto, ResultSet rs) throws SQLException
	{
		dto.setCompetenceCode( rs.getString( COLUMN_COMPETENCE_CODE ) );
		dto.setDescription( rs.getString( COLUMN_DESCRIPTION ) );
		dto.setProgramCode( rs.getString( COLUMN_PROGRAM_CODE ) );
		dto.setVersion( rs.getString( COLUMN_VERSION ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(Competence dto)
	{
	}

	/** 
	 * Returns all rows from the competence table that match the specified arbitrary SQL statement
	 */
	public Competence[] findByDynamicSelect(String sql, Object[] sqlParams) throws CompetenceDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new CompetenceDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the competence table that match the specified arbitrary SQL statement
	 */
	public Competence[] findByDynamicWhere(String sql, Object[] sqlParams) throws CompetenceDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new CompetenceDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
