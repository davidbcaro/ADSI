/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package co.edu.sena.controler.dao.jdbc;

import co.edu.sena.controler.dao.dao.*;
import co.edu.sena.controler.dao.factory.*;
import co.edu.sena.controler.dao.dto.*;
import co.edu.sena.controler.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class EnvironmentDaoImpl extends AbstractDAO implements EnvironmentDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT number_environment, description, status, headquarters_name FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( number_environment, description, status, headquarters_name ) VALUES ( ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET number_environment = ?, description = ?, status = ?, headquarters_name = ? WHERE number_environment = ? AND headquarters_name = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE number_environment = ? AND headquarters_name = ?";

	/** 
	 * Index of column number_environment
	 */
	protected static final int COLUMN_NUMBER_ENVIRONMENT = 1;

	/** 
	 * Index of column description
	 */
	protected static final int COLUMN_DESCRIPTION = 2;

	/** 
	 * Index of column status
	 */
	protected static final int COLUMN_STATUS = 3;

	/** 
	 * Index of column headquarters_name
	 */
	protected static final int COLUMN_HEADQUARTERS_NAME = 4;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 4;

	/** 
	 * Index of primary-key column number_environment
	 */
	protected static final int PK_COLUMN_NUMBER_ENVIRONMENT = 1;

	/** 
	 * Index of primary-key column headquarters_name
	 */
	protected static final int PK_COLUMN_HEADQUARTERS_NAME = 2;

	/** 
	 * Inserts a new row in the environment table.
	 */
	public EnvironmentPk insert(Environment dto) throws EnvironmentDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setString( index++, dto.getNumberEnvironment() );
			stmt.setString( index++, dto.getDescription() );
			if (dto.isStatusNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setShort( index++, dto.getStatus() );
			}
		
			stmt.setString( index++, dto.getHeadquartersName() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EnvironmentDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the environment table.
	 */
	public void update(EnvironmentPk pk, Environment dto) throws EnvironmentDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setString( index++, dto.getNumberEnvironment() );
			stmt.setString( index++, dto.getDescription() );
			if (dto.isStatusNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setShort( index++, dto.getStatus() );
			}
		
			stmt.setString( index++, dto.getHeadquartersName() );
			stmt.setString( 5, pk.getNumberEnvironment() );
			stmt.setString( 6, pk.getHeadquartersName() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EnvironmentDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the environment table.
	 */
	public void delete(EnvironmentPk pk) throws EnvironmentDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setString( 1, pk.getNumberEnvironment() );
			stmt.setString( 2, pk.getHeadquartersName() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EnvironmentDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the environment table that matches the specified primary-key value.
	 */
	public Environment findByPrimaryKey(EnvironmentPk pk) throws EnvironmentDaoException
	{
		return findByPrimaryKey( pk.getNumberEnvironment(), pk.getHeadquartersName() );
	}

	/** 
	 * Returns all rows from the environment table that match the criteria 'number_environment = :numberEnvironment AND headquarters_name = :headquartersName'.
	 */
	public Environment findByPrimaryKey(String numberEnvironment, String headquartersName) throws EnvironmentDaoException
	{
		Environment ret[] = findByDynamicSelect( SQL_SELECT + " WHERE number_environment = ? AND headquarters_name = ?", new Object[] { numberEnvironment, headquartersName } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the environment table that match the criteria ''.
	 */
	public Environment[] findAll() throws EnvironmentDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY number_environment, headquarters_name", null );
	}

	/** 
	 * Returns all rows from the environment table that match the criteria 'headquarters_name = :headquartersName'.
	 */
	public Environment[] findByHeadquarters(String headquartersName) throws EnvironmentDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE headquarters_name = ?", new Object[] { headquartersName } );
	}

	/** 
	 * Returns all rows from the environment table that match the criteria 'number_environment = :numberEnvironment'.
	 */
	public Environment[] findWhereNumberEnvironmentEquals(String numberEnvironment) throws EnvironmentDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE number_environment = ? ORDER BY number_environment", new Object[] { numberEnvironment } );
	}

	/** 
	 * Returns all rows from the environment table that match the criteria 'description = :description'.
	 */
	public Environment[] findWhereDescriptionEquals(String description) throws EnvironmentDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE description = ? ORDER BY description", new Object[] { description } );
	}

	/** 
	 * Returns all rows from the environment table that match the criteria 'status = :status'.
	 */
	public Environment[] findWhereStatusEquals(short status) throws EnvironmentDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE status = ? ORDER BY status", new Object[] {  new Short(status) } );
	}

	/** 
	 * Returns all rows from the environment table that match the criteria 'headquarters_name = :headquartersName'.
	 */
	public Environment[] findWhereHeadquartersNameEquals(String headquartersName) throws EnvironmentDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE headquarters_name = ? ORDER BY headquarters_name", new Object[] { headquartersName } );
	}

	/**
	 * Method 'EnvironmentDaoImpl'
	 * 
	 */
	public EnvironmentDaoImpl()
	{
	}

	/**
	 * Method 'EnvironmentDaoImpl'
	 * 
	 * @param userConn
	 */
	public EnvironmentDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "joycd.environment";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected Environment fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			Environment dto = new Environment();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected Environment[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			Environment dto = new Environment();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		Environment ret[] = new Environment[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(Environment dto, ResultSet rs) throws SQLException
	{
		dto.setNumberEnvironment( rs.getString( COLUMN_NUMBER_ENVIRONMENT ) );
		dto.setDescription( rs.getString( COLUMN_DESCRIPTION ) );
		dto.setStatus( rs.getShort( COLUMN_STATUS ) );
		if (rs.wasNull()) {
			dto.setStatusNull( true );
		}
		
		dto.setHeadquartersName( rs.getString( COLUMN_HEADQUARTERS_NAME ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(Environment dto)
	{
	}

	/** 
	 * Returns all rows from the environment table that match the specified arbitrary SQL statement
	 */
	public Environment[] findByDynamicSelect(String sql, Object[] sqlParams) throws EnvironmentDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EnvironmentDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the environment table that match the specified arbitrary SQL statement
	 */
	public Environment[] findByDynamicWhere(String sql, Object[] sqlParams) throws EnvironmentDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new EnvironmentDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
